# Invariants

The law of multivac itself: the brain is this repo, the code is this repo.
Paths in the `source` column are relative to this file, which lives where
everything multivac owns lives: under `.multivac/`.
Every row is anchored to the source that makes it true; `multivac verify`
checks them on every commit.

| ID | statement | authority | state | date | source |
| --- | --- | --- | --- | --- | --- |
| MV-01 | `verify`, `doctor`, and `doors` never touch the network: no git clone/fetch in their source paths. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-01 brain:src/commands/{verify,doctor,doors}.ts /'(clone|fetch)'/ absent -->
<!-- @anchor MV-01 brain:src/anchor/** /'(clone|fetch)'/ absent -->
<!-- @anchor MV-01 brain:src/lib/** /'(clone|fetch)'/ absent -->
| MV-02 | Exactly three runtime dependencies: picomatch, yaml and citty. A fourth is a design change, not a convenience. **Amended 2026-08-18 by MV-104**: the row read *two … a third is a design change*, and the third arrived as exactly that — a decision taken in the open, with this row and the constitution moved before the package was added, not after. What the row guards is transitive weight, and it is checked the same way at three: citty is one package with zero dependencies of its own. A row that said *as few as possible* would check nothing; a number refuses the next one on the same terms. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-02 brain:package.json /"(citty|picomatch|yaml)": "/ count=3 -->
<!-- @anchor MV-02 brain:package.json /"dependencies":/ unique -->
<!-- @anchor MV-02 brain:test/invariants/deps.test.ts /'picomatch', 'yaml'/ -->
| MV-03 | Git runs with an argument vector — `execFile` for a one-shot, `spawn` for a stream — never through a shell: no `exec(`, no `execSync`, no `shell: true`. | specified | active | 2026-08-15 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-03 brain:src/lib/git.ts /execFile/ -->
<!-- @anchor MV-03 brain:src/lib/git.ts /exec\(|execSync|shell:[[:space:]]*true/ absent -->
| MV-04 | multivac never fabricates git identity: no writes to user.name or user.email anywhere in the source. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-04 brain:src/** /user\.(name|email)/ absent -->
| MV-05 | The anchor dialect gate rejects PCRE shorthand classes at write time with a translation hint. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-05 brain:src/lib/regex.ts /is not POSIX ERE/ -->
<!-- @anchor MV-05 brain:src/lib/regex.ts /RegexDialectError/ -->
| MV-06 | A broken or vacuous leg in a blocking mode exits 1 — the exit matrix has no second answer. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-06 brain:src/commands/verify.ts /cfg\.blocking\.includes/ -->
<!-- @anchor MV-06 brain:src/commands/verify.ts /gating\.size > 0/ -->
| MV-07 | The tombstone cannot be unblocked: config refuses a `blocking:` list without `absent`. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-07 brain:src/lib/config.ts /must include "absent"/ -->
| MV-08 | **Development** is pnpm-only, guarded at preinstall — and the guard never reaches a consumer. It fires only when this package is itself the project being installed (`INIT_CWD` equals the package directory), so `npm i -g multivac` and `npx multivac` install normally while `npm install` inside the repo is refused with the pnpm line. The check is inline and offline: the previous guard shelled out to `npx only-allow pnpm`, which hit the registry on every install AND ran on every consumer's machine, refusing the install of a tool whose whole first instruction is `npx multivac init`. A tool that rejects its users' package manager is a tool nobody installs. | specified | active | 2026-08-16 | [changes/the-first-release.md](changes/archive/the-first-release.md) |
<!-- @anchor MV-08 brain:package.json /INIT_CWD===process\.cwd\(\)/ unique -->
<!-- @anchor MV-08 brain:package.json /only-allow/ absent -->
<!-- @anchor MV-08 brain:site/content/docs/guide/install.md /That guard is scoped to the repo/ -->
| MV-09 | Verify in a repo without `.multivac/config.yml` resolves the brain through the mount, scopes to that repo's anchors plus `*` anchors, same exit matrix. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-09 brain:src/commands/verify.ts /findMount/ -->
<!-- @anchor MV-09 brain:src/commands/verify.ts /resolveRepoKey/ -->
<!-- @anchor MV-09 brain:test/verify/consumer.test.ts /scoped/ -->
| MV-10 | With `staleness: block`, a pin behind the declared channel is a blocking verify failure (exit 1) naming the sync command; the default stays `report`, and an unresolvable channel ref reports, never gates. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-10 brain:src/lib/config.ts /staleness/ -->
<!-- @anchor MV-10 brain:src/commands/verify.ts /staleness[[:space:]]*===[[:space:]]*'block'/ -->
<!-- @anchor MV-10 brain:test/verify/verify.test.ts /staleness:[[:space:]]*block/ -->
| MV-11 | `doors` installs the pre-push shim with `--strict` when `strict_pre_push: true`; the default remains the default-policy shim. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-11 brain:src/lib/config.ts /strict_pre_push/ -->
<!-- @anchor MV-11 brain:src/commands/doors.ts /strictPrePush/ -->
<!-- @anchor MV-11 brain:test/doors/doors.test.ts /strict_pre_push/ -->
| MV-12 | A repo entry whose path resolves to the brain root IS the brain: brain door, never a consumer door, and no mount or pin check. `brain` is a first-class repo key in config, change files and anchors; `*` stays reserved. Spelling is not identity: two keys naming one tree — through `.`, `..` or a symlink — are one evaluation target, so a `*` leg never counts the same file twice. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-12 brain:src/lib/config.ts /const isBrain = samePath/ -->
<!-- @anchor MV-12 brain:src/lib/paths.ts /export const samePath/ -->
<!-- @anchor MV-12 brain:src/anchor/evaluate.ts /const at = \(h: RepoHandle/ -->
<!-- @anchor MV-12 brain:src/commands/doors.ts /entry\.isBrain/ -->
<!-- @anchor MV-12 brain:src/commands/doctor.ts /brain==code/ -->
<!-- @anchor MV-12 brain:src/commands/change.ts /reserved handle for the brain/ -->
<!-- @anchor MV-12 brain:test/repos/brain-first-class.test.ts /brain==code/ -->
<!-- @anchor MV-12 brain:test/repos/brain-first-class.test.ts /a symlinked alias is the same tree/ -->
| MV-13 | `change apply` bases each branch on the newer of the default branch and its remote-tracking ref, offline, and prints the base with its sha and why. The default branch is what git already knows — `origin/HEAD`, then `init.defaultBranch`, then main, then master — and only with none of them does it fall back to HEAD, naming the checked-out branch it is building on. The change's bookkeeping is committed before any branch is made, so every checkout apply hands back inherits it from the base; anything uncommitted in a tree apply would switch is refused by name with the unblocking command, and an existing branch is reused. | specified | active | 2026-08-14 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-13 brain:src/commands/change.ts /merge-base.*--is-ancestor/ -->
<!-- @anchor MV-13 brain:src/commands/change.ts /function baseNames/ -->
<!-- @anchor MV-13 brain:src/commands/change.ts /branching from the checked-out/ -->
<!-- @anchor MV-13 brain:test/change/apply-base.test.ts /inherits the committed declaration/ -->
<!-- @anchor MV-13 brain:src/commands/change.ts /uncommitted work would be overwritten/ -->
<!-- @anchor MV-13 brain:test/change/apply-base.test.ts /local main is ahead/ -->
<!-- @anchor MV-13 brain:test/change/apply-base.test.ts /neither main nor master/ -->
| MV-14 | The hook shim resolves a runnable multivac in order — `mvac` on PATH, `npx --no-install multivac`, repo-local `node dist/cli.js` found from the hook itself — and with none of them warns on stderr and exits 0, never blocking the commit. Runnable means installed: a `dist/` with no `node_modules` beside it is not a runner, because node exits 1 on its first bare import and that exit blocks the commit. `doctor` reports the same order: hooks active (naming the runner) or INACTIVE with the fix. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-14 brain:src/hooks/install.ts /exec npx --no-install multivac/ -->
<!-- @anchor MV-14 brain:src/hooks/install.ts /root\/dist\/cli\.js/ -->
<!-- @anchor MV-14 brain:src/hooks/install.ts /-d "\$root\/node_modules"/ -->
<!-- @anchor MV-14 brain:src/commands/doctor.ts /findRunner/ -->
<!-- @anchor MV-14 brain:test/init/hook-shim.test.ts /never wedge a commit/ -->
<!-- @anchor MV-14 brain:test/init/hook-shim.test.ts /never a blocked commit/ -->
| MV-15 | Claim prose survives the frontmatter: any statement — colons like `staleness: block`, hashes, quotes, newlines, leading dashes — round-trips through serialize/parse unchanged and unreflowed, and a frontmatter YAML error names the offending line and the quoting fix instead of the raw parser message. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-15 brain:src/change/file.ts /lineWidth:[[:space:]]*0/ -->
<!-- @anchor MV-15 brain:src/change/file.ts /quotedRewrite/ -->
<!-- @anchor MV-15 brain:test/change/file.test.ts /statement:[[:space:]]staleness:[[:space:]]block/ -->
| MV-16 | When an anchor's glob matches no tracked file but an untracked file on disk would match it, verify says the file exists untracked and names `git add <path>`, never "fix the glob", and no self-heal rewrites that glob elsewhere. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-16 brain:src/lib/git.ts /--others/ -->
<!-- @anchor MV-16 brain:src/anchor/evaluate.ts /file exists but is untracked/ -->
<!-- @anchor MV-16 brain:test/verify/verify.test.ts /not "fix the glob"/ -->
| MV-17 | A claim listed by an open `.multivac/changes/<slug>.md` is pending: its failing legs report as pending naming that change, never block (not even under `--strict`, not even in a blocking mode), and are never chased by self-heal. A closed or archived change confers nothing. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-17 brain:src/commands/verify.ts /openChangeClaims/ -->
<!-- @anchor MV-17 brain:src/anchor/evaluate.ts /pendingBy/ -->
<!-- @anchor MV-17 brain:test/verify/verify.test.ts /confers nothing/ -->
| MV-18 | The lifecycle reports what it knows: `plan` checks `invariants.adds` against the law table the way it checks touches and retires; `land` records `--landed` against local evidence — the change branch contained in the default branch — and, when it has none, says so as the ordinary fact it is ("no local merge commit to confirm it", normal for a squashed or remote-merged MR) rather than as a warning; `close` ends by naming the commit that stores the archive. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-18 brain:src/commands/change.ts /already in \$\{LAW_PATH\}/ -->
<!-- @anchor MV-18 brain:src/commands/change.ts /no local merge commit to confirm it/ -->
<!-- @anchor MV-18 brain:src/commands/change.ts /archived — commit this/ -->
<!-- @anchor MV-18 brain:test/change/lifecycle-polish.test.ts /no local merge commit to confirm it/ -->
| MV-19 | The anchor include/exclude globs are picomatch patterns over repo-relative paths (`**` crosses directories, `{a,b}` alternates, dotfiles match) — stated in the design and the site's anchor grammar, and named in the parse error that rejects a malformed repo spec. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-19 brain:src/anchor/parse.ts /picomatch patterns/ -->
<!-- @anchor MV-19 brain:DESIGN.md /glob dialect is picomatch/ -->
<!-- @anchor MV-19 brain:site/content/docs/guide/writing-anchors.md /picomatch pattern/ -->
<!-- @anchor MV-19 brain:test/anchor/parse.test.ts /picomatch/ -->
| MV-20 | One predicate decides whether a diagnostic gates, and every printed number and the exit code read it: a line marked blocking always exits 1, a broken or vacuous leg that is not marked never gates on its own, and the summary line counts exactly the marked lines — `--strict` included. A claim an open change holds pending is named in that summary too: exit 0 is the grace, silence is not. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-20 brain:src/commands/verify.ts /function legGates/ -->
<!-- @anchor MV-20 brain:src/commands/verify.ts /gating\.has\(l\)/ -->
<!-- @anchor MV-20 brain:src/commands/verify.ts /const blocking = gating\.size \+ staleBlocking/ -->
<!-- @anchor MV-20 brain:src/commands/verify.ts /claim.*held pending/ -->
<!-- @anchor MV-20 brain:test/verify/verify.test.ts /cannot disagree/ -->
<!-- @anchor MV-20 brain:test/verify/verify.test.ts /the same predicate the markers do/ -->
| MV-21 | `doctor` names the untracked, non-ignored files that look build-critical — a config file at the repo root, a path a `package.json` script names, a path an anchor's include glob covers — as a warning saying `untracked — git add or ignore`. **The ceiling on the script test, stated 2026-08-17:** it is `scripts.includes(file)`, a substring over the whole scripts block. It therefore **misses a path a script builds by concatenation** and **matches a path that merely appears inside a longer one**. Approximate on purpose — a shell parse is a parser — and the row said it exactly where the code says it roughly. It never gates: doctor's exit code stays the config-validity answer. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-21 brain:src/commands/doctor.ts /untracked — git add or ignore/ -->
<!-- @anchor MV-21 brain:src/commands/doctor.ts /function buildCritical/ -->
<!-- @anchor MV-21 brain:test/doctor/doctor.test.ts /untracked — git add or ignore/ -->
| MV-22 | multivac is MIT licensed: `LICENSE` carries the MIT text and the copyright holder, `package.json` declares `"license": "MIT"`, and the README and the site footer say so and point at the file. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-22 brain:LICENSE /Copyright \(c\) 2026 Pierre Ugaz/ unique -->
<!-- @anchor MV-22 brain:package.json /"license": "MIT"/ -->
<!-- @anchor MV-22 brain:README.md /MIT — see \[LICENSE\]/ -->
<!-- @anchor MV-22 brain:site/i18n/en.yaml /MIT licensed/ -->
<!-- @anchor MV-22 brain:test/invariants/license.test.ts /license is MIT in both/ -->
| MV-23 | SQL statement splitting breaks only on a semicolon outside every literal, dollar-quoted body and comment: `''` escapes keep a literal open, a `$$`/`$tag$` body is closed by its own tag, and a comment's semicolons and quotes are inert. A `$` following an identifier character opens no body at all — it belongs to the identifier — so `a$b$c` does not swallow the rest of the file into one statement. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-23 brain:src/anchor/normalize.ts /function dollarTag/ -->
<!-- @anchor MV-23 brain:src/anchor/normalize.ts /function endOfQuoted/ -->
<!-- @anchor MV-23 brain:test/anchor/normalize.test.ts /a function body keeps its semicolons/ -->
<!-- @anchor MV-23 brain:test/anchor/normalize.test.ts /opens no dollar body/ -->
| MV-24 | Every git repo a test creates is initialised on an explicit `main` through the shared `gitInit` helper, so no assertion in the suite depends on the host's `init.defaultBranch`. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-24 brain:test/helpers/fixture.ts /export function gitInit/ unique -->
<!-- @anchor MV-24 brain:test/helpers/fixture.ts /'init', '-q', '-b', 'main'/ -->
<!-- @anchor MV-24 brain:test/** !test/helpers/fixture.ts /'init', '-q'/ absent -->
<!-- @anchor MV-24 brain:test/helpers/scaffold.test.ts /whatever the host init\.defaultBranch says/ -->
| MV-25 | `change apply` gives each change its own worktree under `.multivac/worktrees/<slug>/<repo>` and prints it; `close` removes it. Where git cannot make one, apply falls back in place and refuses a tree carrying another change's uncommitted work. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-25 brain:src/commands/change.ts /worktreePath/ -->
<!-- @anchor MV-25 brain:src/commands/change.ts /apply will not switch it/ -->
<!-- @anchor MV-25 brain:src/commands/change.ts /function removeWorktrees/ -->
<!-- @anchor MV-25 brain:test/change/concurrency.test.ts /both live at once/ -->
| MV-26 | Invariant IDs are allocated by the tool, never by hand: `change new` reserves the next free ID as a `proposed` row in `.multivac/invariants.md` under an exclusive lock and commits it with the scaffolded declaration in one bookkeeping commit, so a concurrent `new` reads the committed table; `plan` refuses a declared ID another change is holding. | specified | active | 2026-08-14 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-26 brain:src/change/reserve.ts /reserveId/ -->
<!-- @anchor MV-26 brain:src/change/reserve.ts /flag: 'wx'/ -->
<!-- @anchor MV-26 brain:test/change/concurrency.test.ts /must not claim the same id/ -->
<!-- @anchor MV-26 brain:src/commands/change.ts /change open: / -->
<!-- @anchor MV-26 brain:test/change/concurrency.test.ts /both rows committed/ -->
| MV-27 | The ritual is the ecosystem's closing ceremony, written by the team in `.multivac/ritual.md`. multivac runs the verifiable half in `change close` and prints the rest verbatim as a checklist — never verified, never gating; an empty or absent ritual prints nothing. `init` scaffolds the file with one comment saying what belongs there. **Amended 2026-08-18 by MV-98**: the scaffolded template is now `ritualSeed(config)` rather than a fixed `RITUAL_TEMPLATE` — it emits candidates drawn from the declarations, all COMMENTED, so a fresh brain still prints nothing and nothing is asserted on the operator's behalf. Print-only and never verified is unchanged; what moved is only what `init` writes into the file, and the file is still never overwritten. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-27 brain:src/lib/config.ts /RITUAL_PATH = '.multivac\/ritual.md'/ -->
<!-- @anchor MV-27 brain:src/lib/ritual.ts /ritualChecklist/ -->
<!-- @anchor MV-27 brain:src/commands/change.ts /ritualChecklist\(brain\)/ -->
<!-- @anchor MV-27 brain:src/commands/init.ts /ritualSeed\(/ -->
<!-- @anchor MV-27 brain:test/change/ritual.test.ts /prints nothing/ -->
| MV-28 | Every harness multivac integrates with is a registry entry in `src/adapters/registry.ts`: `doors` and `doctor` dispatch on the entry's `kind`, never on its name, and a `native` entry projects nothing beyond the canonical `AGENTS.md`. **Every entry is one multivac can actually own** — there is no `unsupported` kind. A harness whose door cannot be written gets no entry, because an entry is how this tool says "supported": it appears in `--provider`'s legal values, in the reference table, and in the count of what multivac integrates with. `aider` sat there as `unsupported`, carrying a note that explained at length why none of it applied, and read as support to everyone who did not open it. An unknown name already gets the list of what IS supported, which is the answer that helps. | specified | active | 2026-08-16 | [changes/no-mention-what-is-not-supported.md](changes/archive/no-mention-what-is-not-supported.md) |
<!-- @anchor MV-28 brain:src/commands/doors.ts /target === '[a-z]+'/ absent -->
<!-- @anchor MV-28 brain:src/commands/doctor.ts /t.kind === 'native'/ -->
<!-- @anchor MV-28 brain:src/adapters/registry.ts /a named tool reads as a supported one/ unique -->
<!-- @anchor MV-28 brain:src/adapters/registry.ts /^[[:space:]]+kind: 'unsupported',/ absent -->
<!-- @anchor MV-28 brain:src/adapters/registry.ts /\| 'unsupported'/ absent -->
<!-- @anchor MV-28 brain:test/doors/registry.test.ts /every entry is one multivac can actually own/ -->
| MV-29 | The site names no flag the binary does not accept. `doors` takes no flags at all — `--no-symlink` was documentation-only and never parsed — so the string appears nowhere in the site or the source. **Strengthened 2026-08-17 (MV-85):** "takes no flags" used to mean *ignores them*, and this row's leg proved it by pinning the unused `_argv` parameter — the absence of a read standing in for the absence of a surface. `doors` now declares its surface and REFUSES any argument outside it, so the leg moved to that declaration: the positive statement rather than the mechanism that happened to imply it. A reader who passes `--no-symlink` today is told so instead of being ignored. **Amended the same day (MV-86):** the clause "`doors` takes no flags **at all**" is WITHDRAWN, because it is no longer true — `doors --adopt` exists. It was never the claim worth making: what protects a reader is that the site names no flag the binary does not accept, and that is now enforced for every command by MV-85 rather than resting on one command happening to have an empty surface. The surviving claim is the headline plus the `--no-symlink` tombstone, and the leg pins the refusal call rather than the contents of doors' surface, so declaring a legitimate new flag no longer breaks a row about something else. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-29 brain:site/content/** /no-symlink/ absent -->
<!-- @anchor MV-29 brain:src/** /no-symlink/ absent -->
<!-- @anchor MV-29 brain:src/commands/doors.ts /undeclared\('doors', argv,/ unique -->
| MV-30 | Hextra's layout shortcodes emit wrapper HTML, so the site sets `markup.goldmark.renderer.unsafe: true`; without it the hero and the feature grid are stripped to bare text and the landing renders unstyled. | specified | active | 2026-08-13 | [hugo.yaml](../site/hugo.yaml) |
<!-- @anchor MV-30 brain:site/hugo.yaml /unsafe: true/ unique -->
<!-- @anchor MV-30 brain:site/content/_index.md /hextra\/feature-grid/ count=2 -->
| MV-31 | The reference section documents the whole surface: one heading per shipped command, one per configuration key the loader reads, and one per harness entry in the registry. **Amended 2026-08-17:** the clause "including the entries marked unsupported" is WITHDRAWN, because MV-28 removed that kind — "there is no `unsupported` kind" — so this row required a heading for something that cannot exist. It is withdrawn rather than deleted so the next reader can see the reference table shrank by decision and not by neglect. | specified | active | 2026-08-13 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-31 brain:site/content/docs/reference/commands.md /^## `(init|seed|verify|count|doors|doctor|repos|change|help)/ count=9 -->
<!-- @anchor MV-31 brain:site/content/docs/reference/configuration.md /^### `(doors|sdd|sdd_auto|grapher|authorities|blocking|staleness|strict_pre_push|channel|mount|repos)`$/ count=11 -->
<!-- @anchor MV-31 brain:site/content/docs/reference/integrations.md /^## `(agents|claude|cursor|opencode|codex|windsurf|gemini|copilot)`/ count=8 -->
<!-- @anchor MV-31 brain:site/content/docs/concepts/philosophy.md /A paraphrase ages silently/ unique -->
<!-- @anchor MV-31 brain:site/content/docs/reference/hooks.md /universal floor/ -->
<!-- @anchor MV-31 brain:site/content/docs/guide/install.md /^  (count|help)[[:space:]]{2}/ count=2 -->
| MV-32 | Everything multivac creates lives under `.multivac/` — the law, the changes and the machinery; `AGENTS.md` at the repo root is the only exception. `init` migrates a brain that still keeps them at the root, announcing every path before it moves it and using `git mv` so history follows, and it refuses rather than overwrite an occupied target. It never moves a file multivac did not write: a root `invariants.md` or `changes/` counts as multivac's only in a directory that already has `.multivac/config.yml` AND whose file parses as multivac's own law table or change file. Only two files that both parse as multivac's law are ambiguous, and that error names the one that wins; `doctor` reports the legacy layout with the command that fixes it and moves nothing itself. | specified | active | 2026-08-14 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-32 brain:src/lib/config.ts /LAW_PATH = '.multivac\/invariants.md'/ -->
<!-- @anchor MV-32 brain:src/lib/config.ts /CHANGES_DIR = '.multivac\/changes'/ -->
<!-- @anchor MV-32 brain:src/lib/config.ts /export async function legacyLayout/ -->
<!-- @anchor MV-32 brain:src/lib/config.ts /looksLikeOurs/ -->
<!-- @anchor MV-32 brain:src/commands/init.ts /migrateLegacy/ -->
<!-- @anchor MV-32 brain:src/commands/init.ts /git mv, history preserved/ -->
<!-- @anchor MV-32 brain:src/** /join\(brain(Dir)?, 'invariants.md'\)/ absent -->
<!-- @anchor MV-32 brain:test/init/init.test.ts /never migrates files multivac did not write/ -->
<!-- @anchor MV-32 brain:test/init/init.test.ts /byte for byte/ -->
| MV-33 | The identity is the console panel: the mark ships under `site/static/` and is wired as the favicon and as the navbar logo — one copy with its ink pinned, because an `<img>` cannot inherit `currentColor` and the site has one ground (dark only, acid accent). The terminal banner is reachable from `init` alone — `src/lib/banner.ts` is imported by `init` and by nothing else, so `verify`, `doctor`, `doors` and `change` cannot print it. It is suppressed by `--quiet` and when stdout is not a TTY; `NO_COLOR` drops the colour and keeps the drawing. The lamp pattern is a fixed drawing, never a live reading — `init` runs before there is anything to verify. | specified | active | 2026-08-15 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-33 brain:site/static/{mark,mark-dark,favicon,lockup}.svg /<svg/ count=4 -->
<!-- @anchor MV-33 brain:site/hugo.yaml /path: mark-dark.svg/ unique -->
<!-- @anchor MV-33 brain:site/hugo.yaml /displayToggle: false/ unique -->
<!-- @anchor MV-33 brain:site/assets/css/custom.css /--primary-hue: 76deg/ unique -->
<!-- @anchor MV-33 brain:src/lib/banner.ts /pattern is FIXED, not a live reading/ -->
<!-- @anchor MV-33 brain:src/commands/init.ts /banner\(\{/ unique -->
<!-- @anchor MV-33 brain:src/** !src/lib/banner.ts !src/commands/init.ts /banner/ absent -->
<!-- @anchor MV-33 brain:test/init/banner.test.ts /never emitted by any other command/ -->
<!-- @anchor MV-33 brain:test/init/banner.test.ts /NO_COLOR drops the colour, not the banner/ -->
| MV-34 | Governance lives in the repo, not in someone's head: `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` at the root, and GitLab's merge request and issue templates under `.gitlab/`, where GitLab reads them. **Amended 2026-08-18 by MV-98**: the merge request template prompts BOTH the claims and the landing order, and this row anchored only the claims — a rule half-checked. The landing-order prompt now has its own leg. | specified | active | 2026-08-14 | [CONTRIBUTING.md](../CONTRIBUTING.md) |
<!-- @anchor MV-34 brain:CONTRIBUTING.md /## The loop/ present -->
<!-- @anchor MV-34 brain:CODE_OF_CONDUCT.md /confidential issue/ present -->
<!-- @anchor MV-34 brain:.gitlab/merge_request_templates/*.md /Claims made true/ present -->
<!-- @anchor MV-34 brain:.gitlab/merge_request_templates/*.md /Landing order/ present -->
<!-- @anchor MV-34 brain:.gitlab/issue_templates/*.md /multivac doctor/ present -->
| MV-35 | An anchor exclusion may name the repo it applies to — `!<repo>:<glob>` — and then bites only in that declared repo; the bare `!<glob>` keeps its meaning, repo-relative in every repo the leg evaluates. An exclusion naming an undeclared repo is a parse-stage diagnostic that names the key, never a silent no-op; a qualifier in a single-repo leg is legal and redundant. Exclusions still count toward vacuity: a leg whose exclusions remove every candidate file is vacuous. | specified | active | 2026-08-14 | [DESIGN.md](../DESIGN.md) |
<!-- @anchor MV-35 brain:src/anchor/parse.ts /is not !<repo>:<glob>/ -->
<!-- @anchor MV-35 brain:src/lib/glob.ts /export function excludeGlobs/ -->
<!-- @anchor MV-35 brain:src/commands/verify.ts /excludes\.map/ -->
<!-- @anchor MV-35 brain:test/anchor/parse.test.ts /an exclusion may name its repo/ -->
<!-- @anchor MV-35 brain:test/verify/verify.test.ts /a qualified exclusion exempts one repo/ -->
<!-- @anchor MV-35 brain:DESIGN.md /An exclusion may name its repo/ -->
| MV-36 | `init` runs `git check-ignore` on every path it writes. When a repo-level ignore would swallow one, init appends explicit negation lines (`!.multivac/`, `!.multivac/**`, `!AGENTS.md` as needed) to the repo's `.gitignore` under a marker comment — idempotently, printing what it appended — and re-checks. `doctor` reports any still-ignored brain path as a WARNING naming the fix. An invisible brain that reports success is the defect. | specified | active | 2026-08-14 | [changes/archive/init-cannot-lie.md](changes/archive/init-cannot-lie.md) |
<!-- @anchor MV-36 brain:src/commands/init.ts /ensureVisibleToGit/ -->
<!-- @anchor MV-36 brain:src/lib/git.ts /check-ignore/ -->
<!-- @anchor MV-36 brain:src/commands/doctor.ts /IGNORED by \.gitignore/ -->
<!-- @anchor MV-36 brain:test/init/coexist.test.ts /saleor shape: a .* gitignore gets marked negations/ -->
<!-- @anchor MV-36 brain:test/init/coexist.test.ts /an ignored brain path is a WARNING/ -->
<!-- @anchor MV-36 brain:DESIGN.md /check-ignore/ -->
| MV-37 | `init` never silently disarms an existing hook set-up. Before touching `core.hooksPath` it detects `.git/hooks/<name>`, a foreign `core.hooksPath`, `.husky/`, `lefthook.yml` and `.pre-commit-config.yaml`; the shim chains a pre-existing `.git/hooks` hook first and preserves its exit code; a foreign hooksPath is never repointed — the shim installs alongside INTO the directory that hooksPath resolves to (MV-79), where the name is free, and a taken name is a refusal carrying the exact line to add. `init` prints the strategy used; `doctor` reports the coexistence state, reading that same resolved directory. Which directory that is gets decided before the strategy does: this row used to say "installs alongside where the name is free" flatly, which held only while the configured value was relative — an absolute one was concatenated onto the repo root, so nothing landed where git looks and the notice named a directory the shims were not in. | specified | active | 2026-08-16 | [changes/core-hookspath-is-read-the-way-git-reads-it.md](changes/archive/core-hookspath-is-read-the-way-git-reads-it.md) |
<!-- @anchor MV-37 brain:src/hooks/install.ts /installAlongside/ -->
<!-- @anchor MV-37 brain:src/hooks/install.ts /exit code wins/ -->
<!-- @anchor MV-37 brain:src/commands/doctor.ts /never repoints/ -->
<!-- @anchor MV-37 brain:test/init/coexist.test.ts /the repo gate runs first and its exit code wins/ -->
<!-- @anchor MV-37 brain:test/init/coexist.test.ts /refusal names the exact step, file untouched/ -->
<!-- @anchor MV-37 brain:DESIGN.md /never repoint/ -->
| MV-38 | Seed knows where architecture lives, as registry data: policy gates, workspace/build graph, deploy manifests, runtime config, models/schema and decisions/intent are pattern entries in the category registry; fixture, example and vendored trees are excluded; and the report ends with the three open questions every cold adopter hit — debt or intent, law or taste, which authority wins — instantiated against what seed found and handed to the interview. | specified | active | 2026-08-14 | [changes/archive/seed-finds-the-contracts.md](changes/archive/seed-finds-the-contracts.md) |
<!-- @anchor MV-38 brain:src/seed/inventory.ts /'policy gates'/ -->
<!-- @anchor MV-38 brain:src/seed/inventory.ts /pnpm-workspace\.yaml/ -->
<!-- @anchor MV-38 brain:src/seed/inventory.ts /kustomization\.yaml/ -->
<!-- @anchor MV-38 brain:src/seed/inventory.ts /export const EXCLUDES/ -->
<!-- @anchor MV-38 brain:src/commands/seed.ts /open questions/ -->
<!-- @anchor MV-38 brain:src/commands/seed.ts /Debt or intent/ -->
<!-- @anchor MV-38 brain:skills/multivac/references/discovery.md /questions/ -->
<!-- @anchor MV-38 brain:test/seed/seed.test.ts /deploys via helm, kustomize, skaffold/ -->
| MV-39 | `multivac help anchor` teaches the whole grammar from the CLI in one screen: the anchor line, POSIX ERE only with the shorthand replacements named, per-line matching except `.sql` (per normalized statement), `count=N` counted across every file the glob matches (a deletion ratchet, never a universal), exactly one include glob (braces for alternatives), repo-qualified exclusions, and where anchors may live. | specified | active | 2026-08-14 | [changes/the-ramp-is-part-of-the-road.md](changes/archive/the-ramp-is-part-of-the-road.md) |
<!-- @anchor MV-39 brain:src/commands/help.ts /POSIX ERE only/ -->
<!-- @anchor MV-39 brain:src/commands/help.ts /deletion ratchet, never a universal/ -->
<!-- @anchor MV-39 brain:src/commands/help.ts /ONE include glob/ -->
<!-- @anchor MV-39 brain:test/cli/help.test.ts /one screen/ -->
| MV-40 | `multivac count '<repo>:<glob> /re/'` is a dry-run that prints the per-file breakdown and the total through the same parse and scan path verify uses — never a reimplementation — so a `count=N` ratchet is right the first time. It writes nothing and exits 0 when the spec evaluates. | specified | active | 2026-08-14 | [changes/the-ramp-is-part-of-the-road.md](changes/archive/the-ramp-is-part-of-the-road.md) |
<!-- @anchor MV-40 brain:src/commands/count.ts /scanLeg/ -->
<!-- @anchor MV-40 brain:src/commands/count.ts /parseAnchors/ -->
<!-- @anchor MV-40 brain:src/commands/count.ts /never a reimplementation/ -->
<!-- @anchor MV-40 brain:test/cli/count.test.ts /the total is the ratchet verify pins/ -->
| MV-41 | `--help`/`-h` on any subcommand is recognized by the dispatcher before the command runs: usage on stdout, exit 0, no side effect on the tree. | specified | active | 2026-08-14 | [changes/the-ramp-is-part-of-the-road.md](changes/archive/the-ramp-is-part-of-the-road.md) |
<!-- @anchor MV-41 brain:src/cli.ts /before any side effect/ -->
<!-- @anchor MV-41 brain:test/cli/help.test.ts /tree untouched/ -->
<!-- @anchor MV-41 brain:test/cli/help.test.ts /recognized before any side effect/ -->
| MV-42 | verify is readable at the summary: parse diagnostics print above it, the unanchored claim ids are named (never only counted), and a law row in state `drift` records a real, not-yet-fixable finding — its legs report but never gate, and the summary names the drifting ids. Every other row keeps the exit matrix unchanged. | specified | active | 2026-08-14 | [changes/the-ramp-is-part-of-the-road.md](changes/archive/the-ramp-is-part-of-the-road.md) |
<!-- @anchor MV-42 brain:src/commands/verify.ts /=== 'drift'/ -->
<!-- @anchor MV-42 brain:src/commands/verify.ts /unanchored: / -->
<!-- @anchor MV-42 brain:src/commands/verify.ts /ABOVE the summary/ -->
<!-- @anchor MV-42 brain:test/verify/verify.test.ts /a drift row never gates/ -->
<!-- @anchor MV-42 brain:site/content/docs/reference/commands.md /drift/ -->
| MV-43 | `each` is the universal quantifier: a leg in mode `each` holds iff every file its glob matches (after exclusions) contains at least one match, and `each!` iff every such file contains none. A glob matching zero tracked files is a blocking failure (a universal over nothing proves nothing), the failing files are named in the report (first few + count), `.sql` files match per normalized statement as everywhere else, and both forms gate by default alongside `absent` and `count` — `count=N` stays a deletion ratchet and the docs say which measured claims still need the cross-file relation that deliberately does not exist. | specified | active | 2026-08-14 | [changes/each-file-answers.md](changes/archive/each-file-answers.md) |
<!-- @anchor MV-43 brain:src/anchor/parse.ts /modeTok === 'each!'/ -->
<!-- @anchor MV-43 brain:src/anchor/evaluate.ts /case 'each':/ -->
<!-- @anchor MV-43 brain:src/anchor/evaluate.ts /over nothing proves nothing/ -->
<!-- @anchor MV-43 brain:src/lib/config.ts /'absent', 'count', 'each'/ -->
<!-- @anchor MV-43 brain:src/commands/help.ts /each is the universal/ -->
<!-- @anchor MV-43 brain:test/verify/each.test.ts /rogue container/ -->
<!-- @anchor MV-43 brain:site/content/docs/guide/writing-anchors.md /cross-file relation/ -->
<!-- @anchor MV-43 brain:site/content/docs/reference/configuration.md /allowed: present, absent, unique, count, each/ -->
<!-- @anchor MV-43 brain:site/content/_index.md /`present`, `absent`, `unique`, `count`, `each`/ unique -->
| MV-44 | The hook chain arms in every order: when `.pre-commit-config.yaml` exists and `.git/hooks/<name>` does not (the fresh-clone shape — `pre-commit install` refuses while core.hooksPath is set), the shim runs `pre-commit run --hook-stage <stage>` directly and preserves its exit code; with no pre-commit binary it warns loudly on stderr and never blocks; `init` and `doctor` name each arrangement's true state, including the uninstalled binary. | specified | active | 2026-08-15 | [changes/the-chain-arms-either-way.md](changes/archive/the-chain-arms-either-way.md) |
<!-- @anchor MV-44 brain:src/hooks/install.ts /pre-commit run --hook-stage/ -->
<!-- @anchor MV-44 brain:src/hooks/install.ts /preCommitGate/ -->
<!-- @anchor MV-44 brain:src/commands/init.ts /gate will not run until it is/ -->
<!-- @anchor MV-44 brain:src/commands/doctor.ts /preCommitGate/ -->
<!-- @anchor MV-44 brain:test/init/coexist.test.ts /config present, hook absent, binary present/ -->
<!-- @anchor MV-44 brain:test/init/coexist.test.ts /loud warning, never a block/ -->
<!-- @anchor MV-44 brain:test/init/coexist.test.ts /husky arrangement has no such trap/ -->
<!-- @anchor MV-44 brain:DESIGN.md /arms in the other order/ -->
<!-- @anchor MV-44 brain:site/content/docs/reference/hooks.md /arms in every order/ -->
| MV-45 | `change close` releases only reservations the change never used: a `proposed` row is released only when it still carries the scaffolded RESERVED statement, no anchor names its ID, and its source points at the closing change — and the anchor set is read before the archive moves the change file out of tracked sight. | specified | active | 2026-08-15 | [changes/archive/close-keeps-used-reservations.md](changes/archive/close-keeps-used-reservations.md) |
<!-- @anchor MV-45 brain:src/change/reserve.ts /startsWith\('RESERVED by change '\)/ -->
<!-- @anchor MV-45 brain:src/commands/change.ts /before archive moves the change file/ -->
<!-- @anchor MV-45 brain:test/change/concurrency.test.ts /a stated rule survives close/ -->
<!-- @anchor MV-45 brain:test/change/concurrency.test.ts /anchors are read before archive/ -->
<!-- @anchor MV-45 brain:site/content/docs/reference/commands.md /used meaning the rule was stated/ -->
| MV-46 | The lifecycle commits its own bookkeeping, scoped to the change that wrote it: `change new` commits the declaration and the reserved row as one commit on the current branch and refuses a tree that is dirty at those paths; `apply` commits the status bump before branching so every worktree inherits the post-bump truth; every command `close` prints is scoped to the closing slug's paths and picks branch+MR wording when the brain has a remote — `add -A` appears nowhere in the lifecycle. **Made literally true 2026-08-17:** it did not. `greenfield()` swept with `add -A` in a repo the lifecycle had just created — harmless in itself, one file written seconds earlier, but the row said *nowhere*. **Worse was the leg**, `/add -A/ count=1`, whose only match in that file was a COMMENT reading "never `add -A`": the real call spells the flag `['add', '-A']` and was invisible to the pattern, so this row's most emphatic clause was green on every commit since it was written, on evidence that was a sentence about the code. `greenfield` now names the one file it wrote, and the leg is an `absent` over `src/**` for the argv form — which is what somebody sweeping a tree would actually type. An exception nobody can check is how a claim decays: `greenfield` growing a second written file would have widened the sweep under cover of the escape clause. | specified | active | 2026-08-14 | [changes/archive/the-ledger-keeps-itself.md](changes/archive/the-ledger-keeps-itself.md) |
<!-- @anchor MV-46 brain:src/commands/change.ts /function commitBookkeeping/ -->
<!-- @anchor MV-46 brain:src/commands/change.ts /bookkeeping paths carry uncommitted edits/ -->
<!-- @anchor MV-46 brain:src/commands/change.ts /change apply: .* status branched/ -->
<!-- @anchor MV-46 brain:src/** /\['add', '-A'\]/ absent -->
<!-- @anchor MV-46 brain:test/change/concurrency.test.ts /touch only the closing slug/ -->
<!-- @anchor MV-46 brain:test/change/lifecycle-polish.test.ts /the branch\+MR variant/ -->
| MV-47 | `doctor --strict` exits 1 when the enforcement gate is disarmed — the shim missing, `core.hooksPath` not multivac's with no shim chained, or no runnable multivac so the shim no-ops. Bare `doctor` stays a report that exits 0 and describes the degradation; its only other exit 1 is an invalid config/law. The exit contract is stated in `mvac doctor --help`, the site's doctor page and the CLI reference. | specified | active | 2026-08-15 | [changes/archive/the-gate-cannot-lie.md](changes/archive/the-gate-cannot-lie.md) |
<!-- @anchor MV-47 brain:src/commands/doctor.ts /enforcement gate is not armed/ -->
<!-- @anchor MV-47 brain:src/commands/doctor.ts /strict && !hooks\.armed/ -->
<!-- @anchor MV-47 brain:src/commands/doctor.ts /armed: ours && installed && runner/ -->
<!-- @anchor MV-47 brain:test/doctor/doctor.test.ts /--strict exits 1 when the gate is disarmed/ -->
<!-- @anchor MV-47 brain:site/content/docs/reference/commands.md /exits 1 when the enforcement gate is disarmed/ -->
| MV-48 | `count`'s `count=N` summary names the universal it cannot pin: it ends with one line pointing a rule that must hold in every file at `each` and a forbid-everywhere rule at `each!`, via `mvac help anchor` — the deletion-ratchet hole `each` was built to close. The `each`/`each!` breakdown lists the zero-match files an each-author needs. `help anchor`, the site's writing-anchors guide and the count reference carry the same guidance. | specified | active | 2026-08-15 | [changes/archive/the-gate-cannot-lie.md](changes/archive/the-gate-cannot-lie.md) |
<!-- @anchor MV-48 brain:src/commands/count.ts /must hold in every file/ -->
<!-- @anchor MV-48 brain:src/commands/count.ts /each \? scan\.globFiles/ -->
<!-- @anchor MV-48 brain:src/commands/help.ts /must hold across files is each/ -->
<!-- @anchor MV-48 brain:test/cli/count.test.ts /count summary nudges toward each/ -->
<!-- @anchor MV-48 brain:site/content/docs/reference/commands.md /must hold in every file/ -->
<!-- @anchor MV-48 brain:site/content/docs/guide/writing-anchors.md /dry-run nudges you here on purpose/ -->
| MV-49 | `verify` from a consumer repo whose mount directory (`.brain` or `.knowledge`) is present but is not a brain — no `.multivac/config.yml`, because the submodule pin predates the brain's migration or points at the wrong commit — names the stale pin and says to update the submodule or fix the pin; it never advises `init`, which would scaffold a second brain beside the mount. `init` stays the hint only when no mount is in reach at all. The site's verify reference documents the stale-mount message. | specified | active | 2026-08-15 | [changes/archive/the-mount-explains-itself.md](changes/archive/the-mount-explains-itself.md) |
<!-- @anchor MV-49 brain:src/commands/verify.ts /function findStaleMount/ -->
<!-- @anchor MV-49 brain:src/commands/verify.ts /is mounted but is not a multivac brain/ -->
<!-- @anchor MV-49 brain:test/verify/consumer.test.ts /named as a bad pin, never told to run init/ -->
<!-- @anchor MV-49 brain:site/content/docs/reference/commands.md /is mounted but is not a multivac brain/ -->
| MV-50 | `change close` executes the declared grapher's refresh — in the brain and in each declared+present repo the change touched, per-scope grapher falling back to the global one — when the binary is on PATH; an absent binary degrades to the install notice and a failing refresh warns, never failing the close — and that warning quotes the TOOL'S own first stderr lines, not node's `Command failed: <cmd>`, which only repeats the command the same warning prints again and drops the cause the tool wrote down. The refresh module never invokes git, so the artifact is left uncommitted, to land only in dedicated chore commits. The git hook shims run `verify` only, and the site says there is no refresh on the git hook path; close is the safety net for edits made outside a harness, not the mechanism (MV-52). | specified | active | 2026-08-15 | [changes/archive/the-graph-refreshes-itself.md](changes/archive/the-graph-refreshes-itself.md) |
<!-- @anchor MV-50 brain:src/commands/change.ts /await refreshGraph\(s\.name, s\.dir, s\.scope, cfg\.graphers\)/ -->
<!-- @anchor MV-50 brain:src/adapters/refresh.ts /never spawns git/ -->
<!-- @anchor MV-50 brain:src/adapters/refresh.ts /'git'|gitRun/ absent -->
<!-- @anchor MV-50 brain:src/adapters/refresh.ts /binary not found —/ unique -->
<!-- @anchor MV-50 brain:src/adapters/refresh.ts /failed \(\$\{said/ unique -->
<!-- @anchor MV-50 brain:src/adapters/refresh.ts /err\.stderr \?\? ''/ unique -->
<!-- @anchor MV-50 brain:test/change/grapher-refresh.test.ts /doesNotMatch\(out, \/Command failed\// unique -->
<!-- @anchor MV-50 brain:test/change/grapher-refresh.test.ts /artifact changed and stays uncommitted/ -->
<!-- @anchor MV-50 brain:test/change/grapher-refresh.test.ts /never a failed close/ -->
<!-- @anchor MV-50 brain:site/content/docs/reference/graphers-and-sdd.md /no refresh on the git hook/ -->
<!-- @anchor MV-50 brain:site/content/docs/reference/graphers-and-sdd.md /never stages or commits the refreshed artifact/ -->
| MV-51 | SDD steps instruct the agent, never shell out: the registry's SDD adapter specs carry the tool's own steps — chat instructions verified against each tool's own docs (the `/opsx:` commands for opsx; `/speckit.*` for speckit, which has no archive equivalent — the gap is stated, never invented). The lifecycle prints the steps bound to its own point, `<slug>` interpolated, only when `sdd_auto` is on and `--no-sdd` was not passed; it never invokes a fake `<binary> <step>` subcommand. **Corrected 2026-08-17:** this row said the validator was the ONE subprocess it may spawn, and MV-75 deliberately added a second — the SDD's own init, which reaches the network, to break the deadlock where declaring an SDD made the change that installs it unplannable. There are TWO, both the tool's own: its validator, run for its verdict (MV-56), and its declared scaffold, run only from `change` and never from `verify`, `doctor` or `doors`. The invariant that survives untouched is the one the row exists for — **no fake step is ever invoked** — and `sdd.ts` already said "besides the tool's own validator" while these rows did not. The brain door carries the flow so the agent knows it at session start, and doctor reports whether `sdd_auto` is on plus what the agent is expected to run. | specified | active | 2026-08-15 | [changes/archive/the-sdd-tells-the-agent.md](changes/archive/the-sdd-tells-the-agent.md) |
<!-- @anchor MV-51 brain:src/adapters/registry.ts /These are chat commands, not terminal subcommands/ -->
<!-- @anchor MV-51 brain:src/adapters/registry.ts /spec-kit has no archive step/ -->
<!-- @anchor MV-51 brain:src/commands/change.ts /INSTRUCT the agent, never shell out/ -->
<!-- @anchor MV-51 brain:src/adapters/sdd.ts /export const withSlug/ -->
<!-- @anchor MV-51 brain:src/adapters/sdd.ts /A step is never faked by shelling out/ -->
<!-- @anchor MV-51 brain:src/commands/change.ts /execFileP\(bin,/ absent -->
<!-- @anchor MV-51 brain:src/commands/change.ts /\$\{step\} done/ absent -->
<!-- @anchor MV-51 brain:src/doors/brain.ts /Features gate through the/ -->
<!-- @anchor MV-51 brain:src/commands/doctor.ts /flow — \$\{l\}/ -->
<!-- @anchor MV-51 brain:test/change/sdd-gates.test.ts /new prints propose/ -->
<!-- @anchor MV-51 brain:skills/multivac/references/change.md /The SDD flow — the lifecycle instructs, YOU run, the gate checks/ -->
<!-- @anchor MV-51 brain:site/content/docs/reference/graphers-and-sdd.md /chat commands the agent runs/ -->
<!-- @anchor MV-51 brain:site/content/docs/reference/graphers-and-sdd.md /has no agent-run close step/ -->
| MV-52 | The graph refresh follows the agent, not the commit: `doors` installs it as a post-edit hook in every declared target whose registry entry carries `hookConfig.postEdit`, and only when a grapher is declared AND its binary is present — one more entry in the same managed `.claude/settings.json` merge that preserves foreign keys AND foreign entries (MV-74), removed again when the grapher goes away — removed at the granularity multivac owns, so an entry a user shares with our refresh survives with their commands in it. The entry is fire-and-forget (backgrounded, stdio discarded, `exit 0` whatever the tool did) and coalesced behind an atomic lock directory under `.multivac/cache/`, so a per-edit harness cannot thrash a large repo. The git hook shims contain no grapher call at all, the refresh module never spawns git, and doctor names the live path — post-edit hook where the harness has one, `change close` as the net, git hooks never. | specified | active | 2026-08-16 | [changes/archive/the-graph-follows-the-agent.md](changes/archive/the-graph-follows-the-agent.md) |
<!-- @anchor MV-52 brain:src/hooks/install.ts /graph|refresh/ absent -->
<!-- @anchor MV-52 brain:src/doors/settings.ts /export function refreshHookCmd/ -->
<!-- @anchor MV-52 brain:src/doors/settings.ts /graph-refresh\.lock/ -->
<!-- @anchor MV-52 brain:src/doors/settings.ts /is the atomic lock/ -->
<!-- @anchor MV-52 brain:src/commands/doors.ts /binaryPresent\(spec\)/ -->
<!-- @anchor MV-52 brain:src/adapters/registry.ts /postEdit\?: string/ -->
<!-- @anchor MV-52 brain:src/adapters/refresh.ts /'git'|gitRun/ absent -->
<!-- @anchor MV-52 brain:src/commands/doctor.ts /refresh path: / -->
<!-- @anchor MV-52 brain:test/doors/settings.test.ts /backgrounded, coalesced, never a failure/ -->
<!-- @anchor MV-52 brain:test/doors/settings.test.ts /takes our hook, not the entry a user shares with it/ -->
<!-- @anchor MV-52 brain:test/doors/doors.test.ts /harness post-edit entry, git shim untouched/ -->
<!-- @anchor MV-52 brain:test/doors/doors.test.ts /no grapher declared: no refresh entry at all/ -->
<!-- @anchor MV-52 brain:site/content/docs/reference/graphers-and-sdd.md /harness post-edit hook/ -->
<!-- @anchor MV-52 brain:DESIGN.md /The graph refresh follows the agent, not the commit/ -->
<!-- @anchor MV-52 brain:skills/multivac/references/change.md /it follows YOUR edits, not the commit/ -->
| MV-53 | Each context verifies what it is responsible for. A brain-scoped `verify` reads every declared repo at its channel ref — `channel:` on the entry, else the global, else `origin/main` — resolved in that repo and read with `git ls-tree` plus one `git cat-file --batch`, never that repo's working tree: the brain's law is about the ecosystem as published, so a sibling parked on a WIP branch is mid-task, not a violation. The brain's OWN repo is the exception and is always read as a working tree, because that is the commit the run gates. A channel ref that does not resolve falls back to the working tree and says so. A consumer-scoped run is unchanged: the working tree, the content about to be committed there. Every run prints one `read` line per repo naming the ref or the branch and its short sha — for a channel read, how old that ref is (MV-54) — and names a checkout parked off its channel; `--worktree` forces the whole-ecosystem working-tree read; `doctor` carries a `branches` line saying where each repo is parked and whether that is its channel. | specified | active | 2026-08-15 | [changes/archive/each-scope-verifies-its-own.md](changes/archive/each-scope-verifies-its-own.md) |
<!-- @anchor MV-53 brain:src/commands/verify.ts /async function resolveSources/ -->
<!-- @anchor MV-53 brain:src/commands/verify.ts /the channel, as published/ -->
<!-- @anchor MV-53 brain:src/commands/verify.ts /FELL BACK to the working tree/ -->
<!-- @anchor MV-53 brain:src/commands/verify.ts /the brain's own repo, the commit this run gates/ -->
<!-- @anchor MV-53 brain:src/commands/verify.ts /const handles: RepoHandle\[\] = sources\.map/ -->
<!-- @anchor MV-53 brain:src/lib/config.ts /export const channelRef/ -->
<!-- @anchor MV-53 brain:src/lib/config.ts /DEFAULT_CHANNEL = 'origin\/main'/ -->
<!-- @anchor MV-53 brain:src/lib/git.ts /export async function lsTree/ -->
<!-- @anchor MV-53 brain:src/lib/git.ts /export async function catFileBlobs/ -->
<!-- @anchor MV-53 brain:src/anchor/match.ts /readonly ref\?: string/ -->
<!-- @anchor MV-53 brain:src/commands/doctor.ts /async function branchesLine/ -->
<!-- @anchor MV-53 brain:test/verify/scope.test.ts /a sibling parked on a WIP branch does not redden the brain/ -->
<!-- @anchor MV-53 brain:test/verify/scope.test.ts /--worktree reproduces the old whole-ecosystem working-tree behaviour/ -->
<!-- @anchor MV-53 brain:test/verify/scope.test.ts /an unresolvable channel falls back to the working tree and says so/ -->
<!-- @anchor MV-53 brain:test/doctor/doctor.test.ts /doctor names the branch each repo is parked on/ -->
<!-- @anchor MV-53 brain:DESIGN.md /Each context verifies what it is responsible for/ -->
<!-- @anchor MV-53 brain:site/content/docs/reference/commands.md /What each run reads/ -->
<!-- @anchor MV-53 brain:site/content/docs/reference/configuration.md /The ecosystem as published/ -->
<!-- @anchor MV-53 brain:skills/multivac/SKILL.md /Read the .read. lines before you read the verdicts/ -->
| MV-54 | A channel ref is only as true as the last fetch, and every surface says so. `repos sync` fetches every declared repo already on disk as well as cloning the missing ones — a failed clone gates, a failed fetch reports and never gates. A brain-scoped `verify` names the age of each channel ref it read (`last fetch 2h ago`, or `never fetched here`), and a brain==code tree behind its own channel is named on its `read` line and on `doctor`'s `branches` line. `verify`, `doctor` and `doors` still never touch the network (MV-01): freshness is bought only in the explicit command. | specified | active | 2026-08-15 | [changes/sync-fetches-the-channel.md](changes/archive/sync-fetches-the-channel.md) |
<!-- @anchor MV-54 brain:src/commands/repos.ts /'fetch', '--quiet'/ unique -->
<!-- @anchor MV-54 brain:src/commands/repos.ts /could not fetch/ -->
<!-- @anchor MV-54 brain:src/commands/verify.ts /async function brainDrift/ -->
<!-- @anchor MV-54 brain:src/commands/verify.ts /never fetched here/ -->
<!-- @anchor MV-54 brain:src/commands/verify.ts /an out-of-date law judges a current ecosystem/ -->
<!-- @anchor MV-54 brain:test/verify/scope.test.ts /a brain merely ON A BRANCH is not/ -->
<!-- @anchor MV-54 brain:src/commands/doctor.ts /behind its own channel/ -->
<!-- @anchor MV-54 brain:test/repos/sync.test.ts /origin\/main was refreshed by sync/ -->
<!-- @anchor MV-54 brain:test/verify/scope.test.ts /the brain behind its OWN channel says so/ -->
<!-- @anchor MV-54 brain:DESIGN.md /"As published" carries its age/ -->
<!-- @anchor MV-54 brain:site/content/docs/reference/commands.md /The fetch is what keeps `verify` honest/ -->
| MV-55 | An SDD adapter carries the tool's OWN flow, never a fixed triple: `projectSteps` (project-level documents, each with when to revisit) plus an ordered `steps` array of arbitrary length, every step bound to a lifecycle point (`new`/`plan`/`apply`/`land`/`close`) rather than to a propose/apply/archive name. The lifecycle prints the steps of its own point, in order, `<slug>` interpolated; the brain door and `doctor` project the same flow. OpenSpec declares no project-level document — the honest gap is stated, not invented — while spec-kit's constitution is declared with its amendment rule. | specified | active | 2026-08-15 | [changes/the-sdd-gates-its-own-flow.md](changes/archive/the-sdd-gates-its-own-flow.md) |
<!-- @anchor MV-55 brain:src/adapters/registry.ts /steps\?: SddStep\[\]/ -->
<!-- @anchor MV-55 brain:src/adapters/registry.ts /projectSteps\?: SddProjectStep\[\]/ -->
<!-- @anchor MV-55 brain:src/adapters/registry.ts /export type LifecyclePoint/ -->
<!-- @anchor MV-55 brain:src/adapters/registry.ts /OpenSpec has NO project-level document/ -->
<!-- @anchor MV-55 brain:src/adapters/registry.ts /run \/speckit\.constitution in your agent/ -->
<!-- @anchor MV-55 brain:src/adapters/sdd.ts /export const stepsAt/ -->
<!-- @anchor MV-55 brain:src/doors/brain.ts /in that tool's OWN flow/ -->
<!-- @anchor MV-55 brain:src/commands/doctor.ts /flow — \$\{l\}/ -->
<!-- @anchor MV-55 brain:test/doors/registry.test.ts /The flow is ORDERED/ -->
<!-- @anchor MV-55 brain:test/change/sdd-gates.test.ts /its own longer flow drives the lifecycle/ -->
<!-- @anchor MV-55 brain:DESIGN.md /in the SDD's own shape/ -->
<!-- @anchor MV-55 brain:site/content/docs/reference/graphers-and-sdd.md /Each tool's own flow, not a fixed triple/ -->
| MV-56 | Every SDD step declares the artifact that proves it ran, `<slug>` interpolated: `change plan` refuses while the propose-equivalent artifact is missing, `change apply` while the plan/tasks artifact is, `change close` while the archive-equivalent has not happened. Each refusal names the exact agent command to run, the artifact path it looked for and the repos it looked in — the brain plus every declared repo present on disk, since a change's specs often live in the code repo — and each pass names the repo the artifact was found in. A step the tool cannot leave an artifact for is declared `ungateable` with its reason and is never gated — the message says so instead of faking it, and a lifecycle point no step gates says the gate does not exist for this tool. Where the tool ships its own validator its verdict is REUSED (`openspec validate --json`), never reimplemented. **Corrected 2026-08-17:** this row said the lifecycle shells out for validation ONLY; MV-75 added the declared scaffold as a second, deliberate subprocess. It shells out for the tool's own validator and the tool's own scaffold, never to fake an agent-run step — which is the claim that was always the point. `sdd_auto: false` and `--no-sdd` turn every gate off. | specified | active | 2026-08-15 | [changes/the-sdd-gates-its-own-flow.md](changes/archive/the-sdd-gates-its-own-flow.md) |
<!-- @anchor MV-56 brain:src/adapters/sdd.ts /export async function sddGate/ -->
<!-- @anchor MV-56 brain:src/adapters/sdd.ts /is missing — looked in \$\{where\}/ -->
<!-- @anchor MV-56 brain:src/adapters/detect.ts /each root carries the name the config gave it/ -->
<!-- @anchor MV-56 brain:test/change/sdd-gates.test.ts /the gate names the repo it searched/ -->
<!-- @anchor MV-56 brain:src/adapters/sdd.ts /refused — \$\{want\} is missing/ -->
<!-- @anchor MV-56 brain:src/adapters/sdd.ts /then re-run: multivac change \$\{gate\} \$\{slug\}/ -->
<!-- @anchor MV-56 brain:src/adapters/sdd.ts /is not gated — this tool declares no step whose artifact could prove it/ -->
<!-- @anchor MV-56 brain:src/adapters/sdd.ts /ungateable: \$\{step\.ungateable/ -->
<!-- @anchor MV-56 brain:src/adapters/sdd.ts /Reuse the tool's own verdict/ -->
<!-- @anchor MV-56 brain:src/commands/change.ts /gateSdd\(brain, cfg, 'plan', slug, noSdd\)/ -->
<!-- @anchor MV-56 brain:src/commands/change.ts /gateSdd\(brain, cfg, 'apply', slug, noSdd\)/ -->
<!-- @anchor MV-56 brain:src/commands/change.ts /gateSdd\(brain, cfg, 'close', slug, noSdd\)/ -->
<!-- @anchor MV-56 brain:src/adapters/detect.ts /export async function artifactHit/ -->
<!-- @anchor MV-56 brain:test/change/sdd-gates.test.ts /plan REFUSES until proposal\.md exists/ -->
<!-- @anchor MV-56 brain:test/change/sdd-gates.test.ts /the tool's own validator is the verdict, not a reimplementation/ -->
<!-- @anchor MV-56 brain:test/change/sdd-gates.test.ts /close still has no archive step, but its task ledger is read/ -->
<!-- @anchor MV-56 brain:test/change/sdd-gates.test.ts /--no-sdd turns off the steps AND the gates/ -->
<!-- @anchor MV-56 brain:test/doors/registry.test.ts /declare an artifact OR an ungateable reason/ -->
<!-- @anchor MV-56 brain:DESIGN.md /The steps are gated on what the tool really produces/ -->
<!-- @anchor MV-56 brain:site/content/docs/reference/graphers-and-sdd.md /The gate: what the tool really produces/ -->
| MV-57 | The project-level document's CONTENT is never machine-judged; its state is reported: `doctor` names it present or missing with the exact agent command that creates it, refuses to call a tool-scaffolded template "present" while its `placeholder` pattern still matches, and calls it STALE when the law's newest row is newer than the file — a product whose law moved while its constitution did not. **The ceiling on STALE, stated 2026-08-17:** it compares the file's **mtime** against the newest row's date, and git does not record mtimes. A fresh clone stamps every file at checkout, so the document always looks newer than the law and STALE **cannot fire for the reader most likely to need it**. It is a real signal on the machine where the file was actually edited and silent everywhere else. The claim is kept rather than withdrawn because it holds where it fires; what was missing is where it stops. Whether the principles are any good is the part no tool can decide, and no tool here tries. Its PRESENCE is another question and MV-76 gates on it, so this row no longer says "never gated"; what is still true here is that `doctor` never gates — it exits 0 through absent, still-a-template, STALE and fresh alike, and the refusing is `change plan`'s job, not the report's. STALE remains a report everywhere, since the law moving is not proof the principles must. BOTH doors tell the agent to create it if absent — `init` writes the instruction into the door it scaffolds and `doors` into the brain door, because `doors` is a second command and a constitution the agent hears about only on the second command is one nobody writes. | specified | active | 2026-08-16 | [changes/the-sdd-gates-its-own-flow.md](changes/archive/the-sdd-gates-its-own-flow.md) |
<!-- @anchor MV-57 brain:src/commands/doctor.ts /Reported here, never judged/ -->
<!-- @anchor MV-57 brain:src/doors/brain.ts /export function projectLawLines/ -->
<!-- @anchor MV-57 brain:src/commands/init.ts /renderBrainDoor\(cfg, countActiveInvariants/ -->
<!-- @anchor MV-57 brain:test/init/init.test.ts /init carries the sdd project law into the door it writes/ -->
<!-- @anchor MV-57 brain:src/commands/doctor.ts /project law @ \$\{root\.scope\}: \$\{found/ -->
<!-- @anchor MV-57 brain:src/commands/doctor.ts /STALE: the law moved while this did not/ -->
<!-- @anchor MV-57 brain:src/commands/doctor.ts /is still the unfilled template shipped by the tool/ -->
<!-- @anchor MV-57 brain:src/adapters/registry.ts /placeholder\?: string/ -->
<!-- @anchor MV-57 brain:src/commands/doctor.ts /project law — revisit: \$\{p\.revisit\}/ -->
<!-- @anchor MV-57 brain:src/doors/brain.ts /CREATE IT IF ABSENT/ -->
<!-- @anchor MV-57 brain:test/doctor/doctor.test.ts /reported present, missing and stale — doctor never gates/ -->
<!-- @anchor MV-57 brain:skills/multivac/references/change.md /reports the document missing, still-a-template, present, or/ -->
<!-- @anchor MV-57 brain:site/content/docs/reference/graphers-and-sdd.md /The project-level document/ -->
| MV-58 | One grapher refresh runs at a time per directory. Every path that shells a grapher — the harness post-edit hook and `change close` — takes the same `.multivac/cache/graph-refresh.lock`, a `mkdir` on that scope's own checkout. The two paths differ in what they do when it is held: the hook SKIPS, because the refresh already running covers this edit; close WAITS on a bounded poll and then proceeds with a notice, because close is the net for edits made outside a harness and a skipped close leaves the graph stale with nobody left to refresh it. The 30-minute sweep in the hook is a ceiling, not a liveness check — it cannot tell a killed process from a slow one — and the code says so instead of claiming it clears stale locks. | specified | active | 2026-08-15 | [changes/the-small-lies-and-the-shared-lock.md](changes/archive/the-small-lies-and-the-shared-lock.md) |
<!-- @anchor MV-58 brain:src/doors/settings.ts /export const GRAPH_LOCK/ unique -->
<!-- @anchor MV-58 brain:src/doors/settings.ts /cannot tell a killed process from a grapher still indexing/ -->
<!-- @anchor MV-58 brain:src/adapters/refresh.ts /Take the SAME lock the post-edit hook takes/ -->
<!-- @anchor MV-58 brain:src/adapters/refresh.ts /await mkdir\(lock\)/ unique -->
<!-- @anchor MV-58 brain:src/adapters/refresh.ts /Where the hook SKIPS, close WAITS/ -->
<!-- @anchor MV-58 brain:test/change/grapher-refresh.test.ts /close takes the SAME lock the post-edit hook takes, and waits for it/ -->
<!-- @anchor MV-58 brain:site/content/docs/reference/graphers-and-sdd.md /grapher-refresh/ -->
| MV-59 | The registry never invents a grapher's contract. A name absent from `knownGraphers` and from the config's own `graphers:` declarations is UNVERIFIED: `grapherSpec` returns null and every caller — `doctor`, `doors`, `change close` — prints the exact fields to declare instead of deriving `<name>-out/graph.json`, `<name> update .` and `npm i -g <name>` from the name, a shape that matched exactly one of ~47 surveyed tools. An unknown tool becomes usable with no merge request against multivac by declaring `graphers.<name>` in `.multivac/config.yml` with `artifact` and `refresh` (optionally `create`, `binary`, `install`); the binary defaults to the first word of `refresh`, because a tool's binary name is not its adapter name (`depcruise` is not `dependency-cruiser`). An artifact path multivac chose rather than the vendor is named as multivac's choice in the entry's note, AND the refresh shipped beside it writes that exact path in a repo that has never run the tool — a chosen path the shipped command cannot write (`--output-to` creates no directories) is not a choice, it is the same invented path one layer down. A field the vendor's docs do not state says UNVERIFIED rather than carrying a guess; a field they DO state is carried verbatim, because UNVERIFIED on a published fact sends the reader to guess the very name this rule exists to stop guessing — PyPI's `axon` is not `axoniq` the way npm's `graphify` is not `graphifyy`. | specified | active | 2026-08-15 | [changes/the-gaps-that-were-not-gaps.md](changes/archive/the-gaps-that-were-not-gaps.md) |
<!-- @anchor MV-59 brain:src/adapters/registry.ts /export function grapherSpec/ unique -->
<!-- @anchor MV-59 brain:src/adapters/registry.ts /if \(!known && !decl\) return null/ unique -->
<!-- @anchor MV-59 brain:src/adapters/registry.ts /export function unverifiedGrapher/ unique -->
<!-- @anchor MV-59 brain:src/adapters/registry.ts /decl!\.binary \?\? decl!\.refresh\.split/ -->
<!-- @anchor MV-59 brain:src/lib/config.ts /multivac will not guess either/ -->
<!-- @anchor MV-59 brain:src/commands/{doctor,doors}.ts /unverifiedGrapher\(/ each -->
<!-- @anchor MV-59 brain:src/adapters/refresh.ts /unverifiedGrapher\(name\)/ -->
<!-- @anchor MV-59 brain:test/doctor/adapters.test.ts /an unknown grapher is UNVERIFIED — nothing is derived from the name/ -->
<!-- @anchor MV-59 brain:test/doctor/adapters.test.ts /a config-declared grapher is usable without a registry MR/ -->
<!-- @anchor MV-59 brain:test/doctor/adapters.test.ts /the table speaks two graphers, and everything else is UNVERIFIED/ -->
<!-- @anchor MV-59 brain:test/change/grapher-refresh.test.ts /an unverified grapher refuses at close/ -->
<!-- @anchor MV-59 brain:site/content/docs/reference/graphers-and-sdd.md /There is no generic contract/ -->
<!-- @anchor MV-59 brain:site/content/docs/reference/configuration.md /### `graphers`/ -->
| MV-60 | Every finding line names the repo it was found in. A leg's matches, the files that fail an `each`, and the candidate files of an ambiguous self-heal all print as `<repoKey>:<file>[:<line>]`, whether the leg is anchored to one repo or to `*` — an unprefixed `src/cli.ts:42` is ambiguous the moment a second repo is declared. | specified | active | 2026-08-15 | [changes/the-small-lies-and-the-shared-lock.md](changes/archive/the-small-lies-and-the-shared-lock.md) |
<!-- @anchor MV-60 brain:src/anchor/evaluate.ts /Every hit says which repo it came from/ -->
<!-- @anchor MV-60 brain:src/anchor/evaluate.ts /const at = \(key: string, file: string\): string/ unique -->
<!-- @anchor MV-60 brain:src/anchor/evaluate.ts /\$\{star \? `\$\{/ absent -->
<!-- @anchor MV-60 brain:test/verify/each.test.ts /pattern .+api:k8s/ -->
<!-- @anchor MV-60 brain:test/verify/scope.test.ts /pattern .+api:src/ -->
| MV-61 | A grapher multivac ships is one whose **query verbs** it knows, not merely one whose artifact it can rebuild. Every entry in `knownGraphers` carries `queries` — each verb exactly as the agent types it, with one line saying what it answers — and the brain door prints them under the artifact, telling the agent to ask the graph before reading the tree raw. The verbs are printed **verbatim per tool and never paraphrased into a common one**: `graphify query` takes a question in words, `codegraph query` takes a symbol by name, and a door saying "query the graph" is wrong for one of them with no way for the agent to tell which. A tool with no query verb carries no `queries`, and the door states that the artifact is written but nothing reads it back — silence there reads as "no graph", and an invented verb is worse than both. A verb enters the table only after being RUN against the shipped binary, never read off `--help`: graphify's `query` is absent from its own help output and is the most useful verb it has. A grapher declared in config gets no query lines at all, because multivac does not know its verbs and will not guess them. **Amended 2026-08-18 by MV-93**: since MV-90 the graph block is rendered once and printed by BOTH doors — the brain's and every declared repo's — resolved with the grapher that applies there. This row said "the brain door" and had gone stale on the day MV-90 landed. | specified | active | 2026-08-16 | [changes/two-graphers-and-what-each-one-answers.md](changes/archive/two-graphers-and-what-each-one-answers.md) |
<!-- @anchor MV-61 brain:src/adapters/registry.ts /export interface GrapherQuery/ unique -->
<!-- @anchor MV-61 brain:src/adapters/registry.ts /never be paraphrased into a/ -->
<!-- @anchor MV-61 brain:src/adapters/registry.ts /queries\?: GrapherQuery\[\]/ unique -->
<!-- @anchor MV-61 brain:src/adapters/registry.ts /absent from `graphify --help`/ -->
<!-- @anchor MV-61 brain:src/adapters/registry.ts /graphify query "<question>"/ unique -->
<!-- @anchor MV-61 brain:src/adapters/registry.ts /codegraph query <symbol>/ unique -->
<!-- @anchor MV-61 brain:src/doors/brain.ts /export function grapherLines/ unique -->
<!-- @anchor MV-61 brain:src/doors/brain.ts /ASK IT BEFORE READING THE TREE RAW/ unique -->
<!-- @anchor MV-61 brain:src/doors/brain.ts /has NO query command/ unique -->
<!-- @anchor MV-61 brain:test/doctor/adapters.test.ts /a grapher states its own query verbs — they are not interchangeable/ -->
<!-- @anchor MV-61 brain:site/content/docs/reference/graphers-and-sdd.md /### What the graph answers/ -->
| MV-62 | A shipped grapher entry names any network its refresh performs, because that refresh runs from a post-edit hook on every edit. `codegraph` sends anonymous telemetry by default, so its note says so, states what the vendor documents as never collected, and gives the opt-out verbatim. The table's contract — a path in the repo, one command safe to re-run, no model and no network — is a claim about what the entry has been made to be, not a hope; where a tool is only offline after an opt-out, the entry says which command makes it true. | specified | active | 2026-08-16 | [changes/two-graphers-and-what-each-one-answers.md](changes/archive/two-graphers-and-what-each-one-answers.md) |
<!-- @anchor MV-62 brain:src/adapters/registry.ts /TELEMETRY IS ON BY DEFAULT/ unique -->
<!-- @anchor MV-62 brain:src/adapters/registry.ts /codegraph telemetry off/ unique -->
<!-- @anchor MV-62 brain:test/doctor/adapters.test.ts /codegraph names its telemetry, because the refresh runs on every edit/ -->
| MV-63 | A step whose tool keeps a **ledger of its own work** is gated on that ledger, not only on the artifact proving the step ran. Every SDD tool here ships a way to finish a step over its own objection — `openspec archive --yes` prints `Warning: N incomplete task(s) found` and archives regardless — and gating on the artifact alone accepts that outcome silently. A step declaring `unfinished` names the ledger path, an ERE matching a line that means NOT DONE, and its own lifecycle gate; the refusal quotes the open items and names the repo they were found in. `unfinished` carries a gate SEPARATE from the step's, so an `ungateable` step is still checked: whether spec-kit's implement RAN leaves no trace and never will, while whether its task list still has open boxes is a fact on disk. This never claims the work happened — `- [x]` is a character the agent types about itself — only that the tool's own book does not say UNDONE. A missing ledger is neither pass nor fail: absence is not evidence of completion, and the artifact gate already covers a step that owed one. | specified | active | 2026-08-16 | [changes/the-ledger-and-the-link.md](changes/archive/the-ledger-and-the-link.md) |
<!-- @anchor MV-63 brain:src/adapters/registry.ts /A ledger the TOOL ITSELF keeps/ unique -->
<!-- @anchor MV-63 brain:src/adapters/registry.ts /continues over its own warning/ unique -->
<!-- @anchor MV-63 brain:src/adapters/sdd.ts /export const stepsLedgered/ unique -->
<!-- @anchor MV-63 brain:src/adapters/sdd.ts /The ledger pass/ unique -->
<!-- @anchor MV-63 brain:src/adapters/sdd.ts /open item\(s\)/ unique -->
<!-- @anchor MV-63 brain:src/adapters/sdd.ts /Absence here is not evidence of completion/ unique -->
<!-- @anchor MV-63 brain:test/change/sdd-gates.test.ts /opsx: close refuses when the archived change still has open tasks/ -->
<!-- @anchor MV-63 brain:test/change/sdd-gates.test.ts /speckit: the ledger is checked even though implement stays ungateable/ -->
| MV-64 | Archiving a change **repoints the law at the file it just moved**. `close` moves `changes/<slug>.md` into `changes/archive/`, and every row whose source column cited the open path is rewritten to the archived one in the same operation. Only `(changes/<slug>.md)` as a markdown link target is rewritten — prose naming the change is left alone — and a link already pointing into `archive/` is skipped, so the rewrite is idempotent. A brain with no law file yet is a no-op, never an error. The rule exists because the table was disagreeing with its own schema: rows written after an archive used the archived path, rows written while the change was open pointed at nothing, and for a tool whose whole claim is that a citation can be checked, a citation resolving to a missing file is exactly the rot it exists to prevent. | specified | active | 2026-08-16 | [changes/the-ledger-and-the-link.md](changes/archive/the-ledger-and-the-link.md) |
<!-- @anchor MV-64 brain:src/change/file.ts /export async function repointLawLinks/ unique -->
<!-- @anchor MV-64 brain:src/change/file.ts /The move is only half the archive/ unique -->
<!-- @anchor MV-64 brain:src/change/file.ts /prose must not be rewritten/ unique -->
<!-- @anchor MV-64 brain:test/change/file.test.ts /archiving rewrites every law row that cited the open change/ -->
<!-- @anchor MV-64 brain:test/change/file.test.ts /repointing a brain with no law file is a no-op, never a crash/ -->
| MV-65 | **A present artifact that proves nothing is treated as missing.** Existence is the weakest possible proof and some tools give it away: spec-kit's `setup-plan.sh` writes the resolved template straight into `plan.md` as part of STARTING the step, and falls back to `rm -f` + `touch` when it cannot resolve one. So a gated artifact also refuses when it is EMPTY — no declaration needed, a step's artifact is never legitimately empty whatever the tool — and when it is BYTE-IDENTICAL to a template named in the step's `untouched` list, which is checked against the paths the tool really copies from, overrides included. Whole-file equality, never a guessed placeholder: the obvious pin — spec-kit's own `# Implementation Plan: [FEATURE]` heading — is a line the tool never asks anyone to change, so a complete real plan keeps it and a regex on it would refuse honest work forever. What the check does not catch is stated in the entry rather than hidden: an agent that edits one line and stops. | specified | active | 2026-08-16 | [changes/the-ledger-and-the-link.md](changes/archive/the-ledger-and-the-link.md) |
<!-- @anchor MV-65 brain:src/adapters/registry.ts /untouched\?: string\[\]/ unique -->
<!-- @anchor MV-65 brain:src/adapters/registry.ts /a line spec-kit NEVER asks anyone to change/ unique -->
<!-- @anchor MV-65 brain:src/adapters/sdd.ts /async function copiedFrom/ unique -->
<!-- @anchor MV-65 brain:src/adapters/sdd.ts /\$\{found\.rel\} is empty/ unique -->
<!-- @anchor MV-65 brain:src/adapters/sdd.ts /the scaffolding wrote it, nobody has/ unique -->
<!-- @anchor MV-65 brain:test/change/sdd-gates.test.ts /an artifact byte-identical to its template, or empty, is refused/ -->
| MV-66 | **A gate that cannot be evaluated refuses.** When a step declares the tool's own `validate` command and that binary cannot be found, `change` refuses and names the binary, the adapter's install line, and the two switches actually scoped to gating — `--no-sdd` for one run, `sdd_auto: false` for good. It never says "drop `sdd:`": that key also renders the whole SDD flow into the brain door, so removing it deletes the agent's instructions along with the check. Passing instead would leave the gate standing on artifact existence alone wherever the tool is not installed — the same command green on a machine that can check nothing, the quietest way this tool could lie. The binary is looked for on `PATH` and then in `node_modules/.bin` beside the artifact, because a project-local `npm i -D` is an ordinary install shape that never touches `PATH`. | specified | active | 2026-08-16 | [changes/the-ledger-and-the-link.md](changes/archive/the-ledger-and-the-link.md) |
<!-- @anchor MV-66 brain:src/adapters/sdd.ts /A MISSING BINARY IS A FAILURE/ unique -->
<!-- @anchor MV-66 brain:src/adapters/sdd.ts /kind: 'missing'; bin: string/ unique -->
<!-- @anchor MV-66 brain:src/adapters/sdd.ts /node_modules', '\.bin'/ unique -->
<!-- @anchor MV-66 brain:src/adapters/sdd.ts /skip the gates without losing the door/ unique -->
<!-- @anchor MV-66 brain:src/adapters/registry.ts /is a REFUSAL, not a pass/ unique -->
<!-- @anchor MV-66 brain:test/change/sdd-gates.test.ts /a validator that is not installed REFUSES, naming the binary and the install line/ -->
<!-- @anchor MV-66 brain:test/change/sdd-gates.test.ts /a locally-installed validator is found in node_modules\/\.bin, not refused/ -->
| MV-67 | **`close` is no weaker than `plan` and `apply`, and abandonment has its own door.** A change declaring no repos is refused at close exactly as it is at plan and apply — the old check filtered the unlanded out of an empty map and found nothing to complain about, so a change that named no repo archived silently and released its reserved id. Every declared key is resolved through the same lookup its siblings use, because counting keys is not having repos: one invented name satisfied an emptiness test while `plan` and `apply` both reject it. And because `change new` reserves an id before anything is declared, `close --abandon` exists to give that id back: it archives a change that made no claims and landed nothing, verifying nothing, since there is nothing to verify. Without it the only routes out are leaking the id forever or writing a false `status: landed`. | specified | active | 2026-08-16 | [changes/the-ledger-and-the-link.md](changes/archive/the-ledger-and-the-link.md) |
<!-- @anchor MV-67 brain:src/commands/change.ts /The gate is on the declaration, not on its leftovers/ unique -->
<!-- @anchor MV-67 brain:src/commands/change.ts /counting keys is not the same as having repos/ unique -->
<!-- @anchor MV-67 brain:src/commands/change.ts /Abandoning is the other ending/ unique -->
<!-- @anchor MV-67 brain:src/commands/change.ts /--abandon/ -->
<!-- @anchor MV-67 brain:src/change/file.ts /Archived is not missing/ unique -->
<!-- @anchor MV-67 brain:test/change/sdd-gates.test.ts /close refuses a repo key that plan and apply already refuse/ -->
<!-- @anchor MV-67 brain:test/change/sdd-gates.test.ts /--abandon gives the reservation back; the refusal points at it/ -->
| MV-68 | **The published tarball carries the tool and nothing else**, by an allowlist rather than by whatever `.gitignore` happens to exclude. `files` is `dist` and `skills`; npm adds `package.json`, `README.md` and `LICENSE`. `skills` ships because `doors` reads the packaged skill at runtime and says so when it is absent — it is code, not documentation. What must NEVER ship: `.multivac/` (this repo's own brain — its law, its open changes, its archive), `site/`, `test/`, `DESIGN.md`, `.gitlab/`. Before the allowlist a publish would have carried 214 files and 1.4MB of exactly that. **Releases are published by trusted publishing (OIDC), never a token**: GitLab mints a short-lived credential for this project and ref, so no long-lived publish token exists to leak or rotate. The job runs on a `v<semver>` tag only, and refuses unless the tag equals `package.json`'s version — a release is a decision somebody makes, never a side effect of a merge. **A tag runs that job and nothing else**: `test` and `selfverify` skip tags, and the publish job does not re-run them either. The commit a tag names already passed both on its way to main, so repeating them only delays the release and adds a second way for it to fail; the one check that could not have been made earlier is the tag-to-version match, and that is the one it keeps. **Amended 2026-08-17 by MV-88**: the trusted-publishing claim is unchanged and was measured on the v0.6.0 tag — the OIDC exchange succeeded from a self-hosted runner, so no long-lived token exists anywhere in this pipeline. What that release found is a limit one layer down, on the ATTESTATION the exchange produces rather than on the credential: npm's registry verifies a provenance bundle only from a gitlab.com shared runner. MV-88 states what that costs and where it is written. | specified | active | 2026-08-16 | [changes/the-first-release.md](changes/archive/the-first-release.md) |
<!-- @anchor MV-68 brain:package.json /"files":/ unique -->
<!-- @anchor MV-68 brain:.gitlab-ci.yml /aud: "npm:registry\.npmjs\.org"/ unique -->
<!-- @anchor MV-68 brain:.gitlab-ci.yml /CI_COMMIT_TAG =~/ unique -->
<!-- @anchor MV-68 brain:.gitlab-ci.yml /never a side effect of a merge/ -->
<!-- @anchor MV-68 brain:.gitlab-ci.yml /A tag runs the publish job and nothing else/ count=2 -->
<!-- @anchor MV-68 brain:.gitlab-ci.yml /when: never/ count=2 -->
<!-- @anchor MV-68 brain:.gitlab-ci.yml /NPM_TOKEN/ absent -->
| MV-69 | **Every command declares its own `usage`**, and `--help` prints it. A one-line description is a name, not documentation: `multivac init --help` printed nothing about `[dir]`, `--provider`, `--sdd`, `--grapher` or `--quiet`, and five of nine commands were the same. The dispatcher already answered `--help` before running anything; what was missing was data, so the fix is data. Where a flag's legal values come from the registry they are rendered FROM the registry — `--provider`, `--sdd` and `--grapher` list what the tool actually ships, so a new adapter cannot leave the help behind. The rule is enforced as an `each` leg over `src/commands/*.ts`, which is what that mode is for: the next command physically cannot ship without one. | specified | active | 2026-08-16 | [changes/every-command-shows-its-flags.md](changes/archive/every-command-shows-its-flags.md) |
<!-- @anchor MV-69 brain:src/commands/*.ts !brain:src/commands/index.ts /^  usage:/ each -->
<!-- @anchor MV-69 brain:src/types.ts /usage\?: string\[\]/ unique -->
<!-- @anchor MV-69 brain:src/commands/init.ts /grapherNames\.join/ unique -->
<!-- @anchor MV-69 brain:test/cli/help.test.ts /every command prints its own flags and arguments/ -->
| MV-70 | **`init` projects what it declares.** A harness named with `--provider` gets its door, its skill and its harness hooks written in the same run — not a name in `doors:` and a second command the user has to discover. The old split ended with `init` printing "load the multivac skill" after installing no skill, which is the tool contradicting itself in its own last line. `init` already wrote AGENTS.md and armed the git hooks, so "flags configure, they never perform" described nothing that was true. It calls the same `doors` code path rather than growing a second one, and with nothing declared beyond the canonical door it does nothing. **`agents` is never a `--provider` value**: agents.md is the open format every door projects FROM, not a tool anyone could install, and AGENTS.md is written unconditionally. **Amended 2026-08-18 by MV-91**: "projects what it declares" means what the CONFIG declares once one exists. A flag naming a different adapter on a re-run is refused rather than silently winning the door, which it did — the config was kept and the door was written from the flag. | specified | active | 2026-08-16 | [changes/every-command-shows-its-flags.md](changes/archive/every-command-shows-its-flags.md) |
<!-- @anchor MV-70 brain:src/commands/init.ts /Project what was just declared/ unique -->
<!-- @anchor MV-70 brain:src/commands/init.ts /await doorsCommand\.run/ unique -->
<!-- @anchor MV-70 brain:src/commands/init.ts /never a --provider value/ unique -->
<!-- @anchor MV-70 brain:test/init/init.test.ts /init with no provider writes only the canonical door/ -->
<!-- @anchor MV-70 brain:src/commands/init.ts /'--agent'/ absent -->
| MV-71 | **Enumeration yields each tracked file exactly once, and a tree mid-merge says so.** git keeps three index entries for a conflicted path — base, ours, theirs — and `ls-files` prints one line per stage, so every match inside such a file was counted three times: a `count=2` leg reported `found 6` and advised "revert the new occurrence, or ratchet to count=6". Advice that, followed, writes a corrupted number into the law over a merge unrelated to the claim — a miscount arriving with confident advice is worse than a crash. `lsFiles` deduplicates, and every `read` line names any path still unresolved, because a verdict taken mid-merge is about a tree nobody will commit: some files one side, some the other. | specified | active | 2026-08-16 | [changes/ls-files-counts-each-file-once.md](changes/archive/ls-files-counts-each-file-once.md) |
<!-- @anchor MV-71 brain:src/lib/git.ts /'--deduplicate'\]/ unique -->
<!-- @anchor MV-71 brain:src/lib/git.ts /one line per stage/ unique -->
<!-- @anchor MV-71 brain:src/lib/git.ts /export async function unmergedFiles/ unique -->
<!-- @anchor MV-71 brain:src/commands/verify.ts /MID-MERGE/ count=2 -->
<!-- @anchor MV-71 brain:test/lib/unmerged.test.ts /a conflicted file is listed once, not once per merge stage/ -->
<!-- @anchor MV-71 brain:test/lib/unmerged.test.ts /the mid-merge state is reported, not silently judged/ -->
<!-- @anchor MV-71 brain:.gitignore /^node_modules$/ unique -->
| MV-72 | **The skill this repo ships and the skill its own harness reads are one tree.** `doors` writes `.claude/skills/multivac/` as a copy of `skills/multivac/`, and this repo tracks both, so a clone is wired without running `doors` — at the price of two committed copies of one tree. An edit that lands in one and misses the other is invisible to git and to every text anchor here, because both copies still contain whatever string an anchor looks for. No anchor can compare two trees, so the comparison is a test: same file list, same bytes, checked from the source outward. | specified | active | 2026-08-16 | [changes/built-with-itself-includes-the-door.md](changes/archive/built-with-itself-includes-the-door.md) |
<!-- @anchor MV-72 brain:test/invariants/skill-copy.test.ts /byte-identical to its source/ -->
<!-- @anchor MV-72 brain:skills/multivac/SKILL.md /^# multivac — operating protocol$/ unique -->
<!-- @anchor MV-72 brain:.claude/skills/multivac/SKILL.md /^# multivac — operating protocol$/ unique -->
| MV-73 | **`doors` removes what it stops projecting.** The skill install is a copy with no delete pass, so a file removed from the source survived in the projected directory forever — reproduced with a planted `references/STALE.md` that outlived every later run. Harmless while the copy was untracked scratch; MV-72 made both trees tracked, so the stale file is committed and the test that pins the two trees goes red for a file the tool itself refuses to remove. Projection is a mirror, not an accretion: every file under the projected skill directory that the source no longer has is deleted in the same run — a file a user added there too, because nothing on disk says who wrote it — bounded to the directory the registry entry names and never a parent, and skipped entirely when the install ships no source to mirror from, since a prune with nothing to copy back would empty the directory it was meant to fill (2026-08-16). | specified | active | 2026-08-16 | [changes/doors-prunes-what-it-projects.md](changes/archive/doors-prunes-what-it-projects.md) |
<!-- @anchor MV-73 brain:src/commands/doors.ts /rmSync\(join\(dest, rel\)/ -->
<!-- @anchor MV-73 brain:src/commands/doors.ts /mirror\(src, join\(dir, dirname\(skill\)\)\)/ -->
<!-- @anchor MV-73 brain:test/doors/doors.test.ts /a file the source no longer has is deleted from the copy/ -->
<!-- @anchor MV-73 brain:test/doors/doors.test.ts /the prune never walks the parent/ -->
<!-- @anchor MV-73 brain:test/doors/doors.test.ts /a missing packaged skill leaves the projected directory alone/ -->
<!-- @anchor MV-73 brain:test/doors/registry.test.ts /a skill path names multivac's own directory, never a shared one/ -->
| MV-74 | **The managed settings merge owns only the hook it wrote.** Claiming any hook entry whose command merely *contains* the marker, then replacing that entry's whole `hooks` array and rewriting its `matcher`, deleted a user's `--strict`, their second command and their `Bash` matcher in one run — and because the claim stops at the first match, multivac's own entry survived further down the list and `verify` then fired twice per edit. A substring of somebody else's command is not identity, and the unit of ownership is the HOOK, not the entry around it: the gate hook is recognised by its whole command being equal to the one multivac writes, the refresh hook by the lock preamble this module generates (its tail carries the grapher's own command, which an update must be free to change). An update rewrites that hook's `command` and completes the `type` multivac itself writes — a hand-typed hook of ours is repaired, never claimed and left malformed — and touches nothing else: sibling commands keep their order, fields multivac does not write survive, and a `matcher` is written once when multivac creates its own entry and never rewritten. Owning a hook is not covering an event: when every gate hook of ours sits under a matcher multivac did not write, the gate does not run on the tools it gates, so multivac ADDS its own entry beside theirs and says so in a notice naming the event and the matcher — appending is additive and reversible where rewriting somebody's matcher is neither, and leaving the edit gate unwired in silence is the one outcome ruled out. The refresh carries no such requirement: it is the agent's aid, not a gate, and rides wherever its hook sits. Dropping the grapher removes every refresh hook and only the entries thereby left empty. A second gate hook in one event — counted before this merge touches anything, so what it reports is what was found on disk — is REPORTED with its event and count through `doors`' notices and never deleted: once the old merge overwrote a foreign entry the survivor is byte-identical to multivac's own, so the count is provable and the choice is not. | specified | active | 2026-08-16 | [changes/the-merge-keeps-what-it-did-not-write.md](changes/archive/the-merge-keeps-what-it-did-not-write.md) |
<!-- @anchor MV-74 brain:src/doors/settings.ts /for \(const h of hooks\)/ unique -->
<!-- @anchor MV-74 brain:src/doors/settings.ts /command\.includes\(marker\)/ absent -->
<!-- @anchor MV-74 brain:src/doors/settings.ts /mine\.matcher = matcher/ absent -->
<!-- @anchor MV-74 brain:src/doors/settings.ts /m\.hook\.command = command/ unique -->
<!-- @anchor MV-74 brain:src/doors/settings.ts /m\.hook\.type = 'command'/ unique -->
<!-- @anchor MV-74 brain:src/doors/settings.ts /const covers = mine\.some/ unique -->
<!-- @anchor MV-74 brain:src/doors/settings.ts /added its own entry beside yours/ unique -->
<!-- @anchor MV-74 brain:src/commands/doors.ts /merged\.notices/ -->
<!-- @anchor MV-74 brain:test/doors/settings.test.ts /a foreign entry that mentions the marker is left alone/ -->
<!-- @anchor MV-74 brain:test/doors/settings.test.ts /a gate under a matcher we do not own gets ours beside it/ -->
<!-- @anchor MV-74 brain:test/doors/settings.test.ts /typed by hand is completed, not left malformed/ -->
<!-- @anchor MV-74 brain:test/doors/settings.test.ts /an update rewrites one hook, not the entry around it/ -->
<!-- @anchor MV-74 brain:test/doors/settings.test.ts /a duplicate is reported, never deleted/ -->
<!-- @anchor MV-74 brain:test/doors/doors.test.ts /a duplicate gate left by an older doors is printed/ -->
<!-- @anchor MV-74 brain:site/content/docs/reference/hooks.md /owns only the hook it wrote/ -->
<!-- @anchor MV-74 brain:site/content/docs/reference/hooks.md /the gate has to cover what it gates/ -->
| MV-75 | **An adapter declares the scaffold that makes its steps runnable.** Declaring an SDD in a repo where it has never run made the change that installs it unplannable: `plan` refuses without the propose artifact, that artifact comes from a chat command, and the chat command does not exist until the tool's own init has run — so the only exits were `--no-sdd` and `sdd_auto: false`, both of which turn the gate off to fix the reason it fired. An adapter therefore carries the scaffold artifact whose absence means "not installed here" and the vendor's own init command verbatim, verified by running it and never derived from the name (MV-59's rule). The lifecycle runs it when the scaffold is missing, prints it first, and skips when the artifact is there; `verify`, `doctor` and `doors` never do, because that command reaches the network and MV-01 binds them — `doctor` reports the state instead, naming the command the lifecycle will run and saying it is not the one running it. **Corrected 2026-08-17:** "runs it when the scaffold is missing" never said missing WHERE, and the implementation answered the question this row left open in the narrowest way available — it scaffolded `roots[0]`, and only after checking that NO root had the artifact, so one sibling repo initialized by hand suppressed the scaffold everywhere including the brain. Missing is a per-root fact, and MV-87 says so. What survives untouched is the claim this row exists for: the init is the vendor's own, verbatim, run from the change lifecycle alone. | specified | active | 2026-08-16 | [changes/the-sdd-arrives-with-its-own-scaffold.md](changes/archive/the-sdd-arrives-with-its-own-scaffold.md) |
<!-- @anchor MV-75 brain:src/adapters/registry.ts /scaffold\?: SddScaffold/ unique -->
<!-- @anchor MV-75 brain:src/adapters/registry.ts /specify init --here --integration claude/ unique -->
<!-- @anchor MV-75 brain:src/adapters/sdd.ts /export async function runScaffold/ unique -->
<!-- @anchor MV-75 brain:src/commands/{verify,doctor,doors}.ts /runScaffold|scaffoldSdd/ absent -->
<!-- @anchor MV-75 brain:src/commands/doctor.ts /declared but never run here/ unique -->
<!-- @anchor MV-75 brain:test/change/sdd-gates.test.ts /a declared SDD that is not installed scaffolds itself/ -->
<!-- @anchor MV-75 brain:test/doctor/doctor.test.ts /a declared-but-unscaffolded sdd names the init/ -->
| MV-76 | **A project-level document is gated on existing, never on its content.** MV-57 made the whole document a report on the grounds that a constitution cannot be machine-judged — true of whether the principles are any good, false of whether the file is there at all. `doctor` already tells absent from still-the-unfilled-template from STALE, so the machinery exists and nothing acted on it: a door printing CREATE IT IF ABSENT in capitals, and four lifecycle commands running green without it. `change plan` refuses while a declared `projectStep`'s artifact is missing or unreadable — a directory or a broken link is not a written document — empty, or still matching the step's declared `placeholder`. **NOT the whole-file equality MV-65 uses**, and the difference is measured, not stylistic: `copiedFrom` FAILS OPEN when it cannot read the template, on purpose and under test, so an unreadable template would silently pass a document nobody wrote — the exact hole this row exists to close; and MV-65 chose equality because spec-kit's `# Implementation Plan: [FEATURE]` heading is a line the tool never asks anyone to change, whereas the constitution template's `[ALL_CAPS]` tokens are ones `/speckit.constitution` explicitly instructs the author to replace, so the pin MV-65 rejected is the right one here and needs no second file to be readable. Only `plan` gates: it is the first point at which the absence changes the work, and the document is what `/speckit.plan`'s own Constitution Check reads. **MV-75's scaffold satisfies none of it**, and that is why existence alone was never the check: `runScaffold` runs immediately before this gate in the same command, and `specify init` writes the constitution as the UNFILLED template — so a document exists one line before the gate reads it and nobody has written it. Existence-only would have this row closed at the top and reopened one layer down, in a command that prints `scaffolded` and green in the same breath; the `placeholder` pin is what refuses it. A fixture stubbing that init therefore writes the tokens the real one writes, because a stub kinder than the tool tests a tool that does not exist. Staleness stays a report: the law moving is not proof the principles must. No leg names `src/commands/change.ts`: the change drafted one on a `gateProjectLaw` there and it was never written, because every lifecycle point already routes through `sddGate` — the check went in beside the per-step passes and `change.ts` was not touched at all. That is said here rather than left as a leg that quietly vanished. Amends MV-57. **Corrected 2026-08-17:** this row said WHAT the gate checks and never WHERE, and the pass took the first root that could answer — so in an ecosystem of six, one repo's constitution satisfied the gate for all of them and five repos planned against a document they had never seen. The gate asks PER ROOT (MV-87), of every root where the tool is installed, and names each root that fails; roots that opted out with `sdd: none`, and roots the tool has never been scaffolded into, are not asked — refusing a whole ecosystem's work over a document a repo has no reason to own would be a gate nobody could satisfy without scaffolding a repo they deliberately excluded. | specified | active | 2026-08-16 | [changes/the-project-document-is-gated-on-existing.md](changes/archive/the-project-document-is-gated-on-existing.md) |
<!-- @anchor MV-76 brain:src/adapters/sdd.ts /const PROJECT_DOC_GATE: GatePoint = 'plan'/ -->
<!-- @anchor MV-76 brain:src/adapters/sdd.ts /gate === PROJECT_DOC_GATE \? \(spec\.projectSteps \?\? \[\]\) : \[\]/ -->
<!-- @anchor MV-76 brain:src/adapters/sdd.ts /is missing or unreadable — looked in/ -->
<!-- @anchor MV-76 brain:src/adapters/sdd.ts /new RegExp\(doc\.placeholder\)\.test\(body\)/ -->
<!-- @anchor MV-76 brain:src/adapters/sdd.ts /only of roots where this tool is INSTALLED/ unique -->
<!-- @anchor MV-76 brain:test/change/sdd-gates.test.ts /asks every installed root and names each that fails/ -->
<!-- @anchor MV-76 brain:src/adapters/registry.ts /placeholder: '\\\\\[\[A-Z0-9_\]\+\\\\\]'/ -->
<!-- @anchor MV-76 brain:src/doors/brain.ts /CREATE IT IF ABSENT — \\`change plan\\` refuses while it is missing/ -->
<!-- @anchor MV-76 brain:DESIGN.md /gated on existing, never on its/ -->
<!-- @anchor MV-76 brain:test/change/sdd-gates.test.ts /plan refuses while the project document is absent or still the template/ -->
<!-- @anchor MV-76 brain:test/change/sdd-gates.test.ts /a stale project document still reports, never gates/ -->
<!-- @anchor MV-76 brain:test/change/sdd-gates.test.ts /the scaffold does not satisfy the project-document gate it runs in front of/ -->
<!-- @anchor MV-76 brain:test/change/sdd-gates.test.ts /const CONSTITUTION_TEMPLATE/ unique -->
<!-- @anchor MV-76 brain:test/change/sdd-gates.test.ts /an unreadable template must not refuse an authored artifact/ -->
<!-- @anchor MV-76 brain:test/doors/doors.test.ts /CREATE IT IF ABSENT — `change plan` refuses while it is missing/ -->
| MV-77 | **The site advertises the last version PUBLISHED, and states no version of its own.** This row used to read "the version the site advertises is the version the package declares", and it was checked faithfully — against the wrong pair. The badge and `package.json` **both live at HEAD**, so they agreed with each other and neither knew what npm serves. The release sequence is bump → merge → **the site deploys** → tag → publish, which means every release advertised a version nobody could install for as long as that took; and a release abandoned after the bump merged would advertise it **forever**, with this row calling it correct. **Corrected 2026-08-17 by removing the subject rather than checking it harder.** The site states no version: the badge renders `{{< param release >}}`, and the pages job sets it from `git describe --tags --abbrev=0`. What is not written cannot drift, so the guarantee stops depending on a test somebody has to keep honest — and `test/invariants/site-version.test.ts` is **deleted**, because it existed to compare two literals and there is now one fewer than it needs. MV-84 tightens to `count=0` in the same change: **no version literal anywhere under `site/content/`**. **Why the last tag is a sound stand-in for "published", offline.** MV-68 refuses to publish under a tag that disagrees with the manifest, so a tag IS a published version — by refusal, not by inspection, and `verify` may not ask a registry (MV-01). **The limit that refusal leaves, and the mechanism that closes it**: a tag whose publish job FAILED is still the newest tag, so the derivation alone could name it. What prevents that is the stage order — `test → publish → deploy` — under which a release's site follows the publication it describes and does not run if that fails. Those three lines look like formatting and are the guarantee; the file says so beside them. **That ordering carries no anchor, and the reason is structural**: it is a RELATION between lines and the scanner matches one line at a time, so no leg can express "publish appears before deploy". Anchoring the comment that explains it would be MV-46's defect committed knowingly — evidence that is a sentence about the code. It is a test, `test/invariants/site-deploy.test.ts`, which reads the stage list and the pages rules; and it is the only thing this change replaces the deleted version test with. **And why the site still deploys on every merge**: releasing was rejected as the deployment trigger because site-only corrections would then wait for a release nobody needs to cut. Deployment is unchanged; only the number changed its source. **Amended 2026-08-18 by MV-100**: the site now also carries what a reader who has NOT arrived yet sees — a description and a card image on every page, so a pasted link renders as itself instead of as a bare URL. The version badge claim is unchanged. | specified | active | 2026-08-16 | [changes/the-site-quotes-the-version-the-package-declares.md](changes/archive/the-site-quotes-the-version-the-package-declares.md) |
<!-- @anchor MV-77 brain:site/hugo.yaml /release: dev/ unique -->
<!-- @anchor MV-77 brain:site/content/_index.md /<span>v\{\{< param release >\}\}<\/span>/ unique -->
<!-- @anchor MV-77 brain:.gitlab-ci.yml /HUGO_PARAMS_RELEASE=\$\(git describe --tags --abbrev=0/ unique -->
<!-- @anchor MV-77 brain:.gitlab-ci.yml /GIT_DEPTH: 0/ unique -->
<!-- @anchor MV-77 brain:test/invariants/site-deploy.test.ts /publish runs before deploy/ -->
| MV-78 | **The changelog is one file with two surfaces, and a released version has an entry in it.** Two versions shipped saying nothing about what they contained: `changes/archive/` is the brain's ledger, written for the people changing multivac, and organised by decision rather than by release. `CHANGELOG.md` at the repo root is the only copy — the site MOUNTS it (`source: ../CHANGELOG.md`) rather than keeping a second one, so the two surfaces cannot disagree because there is no second file to forget. Declaring that mount replaces Hugo's default content mount, so `content → content` is restated beside it and losing that line empties the site rather than one page. Entries are `## <semver> — <date>`, newest first, and the version `package.json` declares has one: a release nobody wrote a line for is not released, it escaped. That last check is a test, not a leg — a leg pinned to the literal current version would need editing by the same hand that forgot the entry. | specified | active | 2026-08-16 | [changes/the-release-says-what-changed.md](changes/archive/the-release-says-what-changed.md) |
<!-- @anchor MV-78 brain:site/hugo.yaml /source: \.\.\/CHANGELOG\.md/ unique -->
<!-- @anchor MV-78 brain:site/hugo.yaml /target: content\/docs\/changelog\.md/ unique -->
<!-- @anchor MV-78 brain:CHANGELOG.md /^## [0-9]+\.[0-9]+\.[0-9]+ — [0-9]{4}-[0-9]{2}-[0-9]{2}$/ -->
<!-- @anchor MV-78 brain:test/invariants/changelog.test.ts /has an entry for the version the package declares/ -->
<!-- @anchor MV-78 brain:test/invariants/changelog.test.ts /mounts the changelog rather than copying it/ -->
| MV-79 | **`core.hooksPath` is read the way git reads it**, in git's own order. Every read goes through `git config --path`, never plain `git config`: git expands a leading `~` or `~user` to the home directory FIRST, and plain `git config` hands back the literal configured text, so `~/hooks` would arrive as the literal seven characters and resolve against the repo root. What the expansion leaves, if absolute, names that directory; anything else resolves against the worktree root, because git moves there before running a hook. This repo's own `.multivac/hooks` is recognised as multivac's whether it is spelled relatively or absolutely — identity is decided after resolution, never by comparing the configured text to a literal. Resolution is lexical, so a spelling that reaches the same directory through a symlinked prefix reads as foreign: the shims still land in the right place and the gate still fires, but the strategy is reported as `alongside`. That is stated rather than claimed away; MV-12's `samePath` is the mechanism that would close it, and it is not used here yet. One read (`--path` at every call site) and one resolution (`resolveHooksPath`) serve `init` and `doctor`, so the directory the shims are written into is the directory the report reads them from, the directory `--strict` judges, and the directory git will run. Never a path built by joining a repo root onto a value git would have expanded or taken whole: `join` concatenates there, and the shims landed in a tree named after the machine's filesystem — or in a directory literally named `~` inside the checkout — while `init` printed that path as the place they went and `doctor` called them missing from the directory they were sitting in. | specified | active | 2026-08-16 | [changes/core-hookspath-is-read-the-way-git-reads-it.md](changes/archive/core-hookspath-is-read-the-way-git-reads-it.md) |
<!-- @anchor MV-79 brain:src/hooks/install.ts /export function resolveHooksPath/ unique -->
<!-- @anchor MV-79 brain:src/hooks/install.ts /join\(repo, dir/ absent -->
<!-- @anchor MV-79 brain:src/hooks/install.ts /'config', '--path', key/ unique -->
<!-- @anchor MV-79 brain:src/commands/doctor.ts /resolveHooksPath/ -->
<!-- @anchor MV-79 brain:src/commands/doctor.ts /'config', '--path', 'core\.hooksPath'/ unique -->
<!-- @anchor MV-79 brain:test/init/coexist.test.ts /not in a tree built by concatenation/ -->
<!-- @anchor MV-79 brain:test/init/coexist.test.ts /core\.hooksPath expands to \$HOME/ -->
<!-- @anchor MV-79 brain:test/init/coexist.test.ts /our own hooks dir spelled absolutely is ours/ -->
<!-- @anchor MV-79 brain:test/init/coexist.test.ts /doctor reads the resolved directory/ -->
<!-- @anchor MV-79 brain:DESIGN.md /the way git reads it/ -->
<!-- @anchor MV-79 brain:site/content/docs/reference/hooks.md /the way git reads it/ -->
| MV-80 | **A change whose every declared claim already resolves, with every declared repo recorded landed, is finished, not pending.** MV-17 makes a claim listed by an open change pending, and pending never blocks — so nine changes left open, five of them for weeks after their code landed, held **fourteen claims** non-gating while `verify --strict` reported green. The failure conceals itself: the longer a finished change stays open, the longer its rows are unenforced, and the only warning is a line that has scrolled past on every commit since. A change opened five minutes ago with nothing built and a change whose every claim resolves are not the same state, and the difference is decidable from what verify already computes — no remote, no fetch. `--strict` refuses the second, names the slug, and says to close it; a change declaring NO claims is never finished, because a universal over nothing is true of a change scaffolded seconds ago. The landed condition is a **narrowing of the drafted statement, added 2026-08-16 when the code was built**: the gate's only output is `change close <slug>`, and `close` refuses a change with any repo outstanding — a gate that prints an instruction the same binary rejects is how a real line stops being read. It costs nothing against the damage (every change that reaches `close` has all repos landed, so all nine qualified) and it buys silence on an author's own branch, where every claim resolving is the ordinary state of work in progress, not evidence of being done. Landing is read from the **channel ref** (MV-53) rather than from commit containment, which a squashing forge defeats every time, and carries the ref's age (MV-54) so an unresolved claim is never silently read as unlanded when it is merely unfetched. That read **offers** its conclusion and `land --landed` stays the human's assertion — the same date's decision on the question the change left open — because MV-54 makes the negative ambiguous, published content proves publication and not authorship, and the record is committed state. That verdict is per **change**, never per repo (**2026-08-17**): it is one evaluation against one ref, the brain's channel, and a `*` leg belongs to no single repo — so it prints under its own `channel:` label, because behind the repo key `--landed` names it read as the brain's ref and sha being a sibling's evidence, the invented attribution this row's own design note forbids. A channel that does not resolve **says so** rather than going quiet, the same never-guess rule the read line follows. Recording the last repo arms this gate, and CI runs `verify --strict` on the channel, so `land` names the consequence: main stays red until `close` runs. **Limit, inherited and now load-bearing (2026-08-17):** a claim declared by two open changes is attributed to the first change file in filename order, so a genuinely finished second change can be invisible to the gate. Closing stays manual: `close` archives and prints a commit the operator runs, because nothing lands on `main` directly. | specified | active | 2026-08-16 | [changes/a-finished-change-is-not-a-pending-one.md](changes/archive/a-finished-change-is-not-a-pending-one.md) |
<!-- @anchor MV-80 brain:src/commands/verify.ts /finishedChanges\(claims, open\)/ unique -->
<!-- @anchor MV-80 brain:src/commands/verify.ts /opts\.strict === true && finished\.length > 0/ unique -->
<!-- @anchor MV-80 brain:src/commands/verify.ts /gating\.size \+ staleBlocking \+ finishedBlocking/ unique -->
<!-- @anchor MV-80 brain:src/commands/change.ts /the work is published there, however it got in/ -->
<!-- @anchor MV-80 brain:src/commands/change.ts /not landed, or not fetched/ -->
<!-- @anchor MV-80 brain:src/commands/change.ts /`channel: \$\{ch\.line\}`/ unique -->
<!-- @anchor MV-80 brain:src/commands/change.ts /does not resolve here \(no remote, or never fetched\) — nothing read/ unique -->
<!-- @anchor MV-80 brain:src/commands/change.ts /refuses \$\{slug\} as unclosed \(MV-80\), here and in CI/ unique -->
<!-- @anchor MV-80 brain:DESIGN.md /every declared repo landed \| reported, exit 0 \| exit 1/ unique -->
<!-- @anchor MV-80 brain:site/content/docs/reference/commands.md /every declared repo landed \| reported, \*\*0\*\* \| \*\*1\*\*/ unique -->
<!-- @anchor MV-80 brain:test/verify/verify.test.ts /a finished change is refused as unclosed, not excused as pending/ -->
<!-- @anchor MV-80 brain:test/verify/verify.test.ts /a change with work left is still pending, and still does not block/ -->
<!-- @anchor MV-80 brain:test/verify/verify.test.ts /an empty declaration is not finished by vacuity, and neither is an unlanded one/ -->
<!-- @anchor MV-80 brain:test/change/lifecycle-polish.test.ts /land reads landing from the channel: published bytes are the evidence a squash destroys/ -->
<!-- @anchor MV-80 brain:test/change/lifecycle-polish.test.ts /the channel verdict is per change, never behind the repo key --landed names/ -->
<!-- @anchor MV-80 brain:test/change/lifecycle-polish.test.ts /a channel that does not resolve says so, instead of going quiet about landing/ -->
<!-- @anchor MV-80 brain:test/change/lifecycle-polish.test.ts /landing the last repo says the strict gate is now armed, and CI runs it/ -->
| MV-81 | **Who enacts a row is not a fact on disk.** The rule that an agent proposes and a human enacts governs who may change the law, and `verify` cannot check it: multivac never fabricates git identity (MV-04), so an agent commits as the person — seven rows went `proposed → active` in this repo with the human's name on every commit — and git hooks execute with the caller's permissions, so a gate installed at pre-commit is one the same process can skip. A guardrail cannot live on the side of the thing it stops. The row therefore declares itself **ungateable with its reason**, the shape this project already uses for a step no artifact can prove, and names where enforcement actually is: the forge's merge button, held by an account the agent does not have. The half that IS checkable is not who but when — a state change to `active` lands in its own commit, never beside the code it anchors, because a rule and its evidence arriving together under one hand is a rule nobody reviewed on its own. A row absent from the previous commit and born `active` is the same offence, caught the same way. **The limit, stated rather than implied** (amended 2026-08-16, in the change that built it — the drafted row named the rule and its ungateability but not what the check can reach): `verify` reads tracked files, which show a state and never a state *change*, so this half is decided from **the index against HEAD** — the only offline comparison that does not invent a reference — and it can therefore answer only while a commit is being composed. Outside one it prints that it could not answer, with the reason, rather than passing quietly; a check that silently does nothing is worse than one that says it did not apply. It is not a security boundary: nothing here stops a person with push rights who decides to skip it, and the rule addresses the ordinary path and the unsupervised agent — the second being the real case, because the agent does not hold the button. The declaration is printed where a reader meets it: `doctor` carries it beside the line that reports what IS armed, so silence there can never read as coverage. | specified | active | 2026-08-16 | [changes/enactment-is-gated-where-the-credential-lives.md](changes/archive/enactment-is-gated-where-the-credential-lives.md) |
<!-- @anchor MV-81 brain:src/commands/verify.ts /enactment lands in its own commit/ -->
<!-- @anchor MV-81 brain:src/commands/verify.ts /who enacts is not a fact on disk/ -->
<!-- @anchor MV-81 brain:src/commands/verify.ts /'--cached'/ unique -->
<!-- @anchor MV-81 brain:src/commands/verify.ts /the index against HEAD/ -->
<!-- @anchor MV-81 brain:src/commands/verify.ts /not answered/ -->
<!-- @anchor MV-81 brain:src/commands/doctor.ts /label\('enact'\) \+ ENACTMENT_UNGATEABLE/ -->
<!-- @anchor MV-81 brain:test/verify/verify.test.ts /a row enacted beside the code it anchors is refused/ -->
<!-- @anchor MV-81 brain:test/verify/verify.test.ts /the enactment check says when it could not answer/ -->
<!-- @anchor MV-81 brain:site/content/docs/concepts/invariants.md /ungateable/ -->
| MV-82 | **A leg skips a line only when that line carries a COMPLETE anchor comment: the opener the grammar defines, and the `-->` that closes it.** What the guard is FOR: an anchor line carries its own regex TEXT, so when a leg scans a file that holds anchors — this table, a change file, DESIGN.md, the guide page teaching the grammar, or a test fixture built from anchor strings — that text must not satisfy or break another leg. What it must NOT become: a test for any fragment shorter than the whole comment. It was first the bare substring `@anchor`, which made every line of every file of every type that merely mentioned the word invisible to every leg — `const evade = "user.name"; // @anchor` in `src/` passed MV-04's tombstone at exit 0 while the same line without the seven-character suffix broke it. Narrowing that to the OPENER alone left the same defect standing in further spellings, six of them nothing but a missing terminator: the opener with nothing closing it, with zero whitespace, with a tab, with a non-word character satisfying `\b`, and the opener sitting inside a plain or a template string literal that is not a comment at all. For a tool whose only value is that a gate ran, any fragment that silences an `absent`, `unique`, `count` or `each!` leg per line is the worst defect available, and it is easiest to reach for exactly when a gate is in the way. The opener is therefore ONE exported recogniser, `ANCHOR_LINE`, and both readers of that shape use it: `parseAnchors`, which decides a line is trying to declare a leg, and `matchesInFile`, which decides a line contributes no matches. What the two share is the PATTERN, not the verdict, and the weaker claim is the true one: `collectBrainAnchors` parses only the handful of `.md` files it reads, while the scanner reads every tracked file in every declared repo, so the lines the law calls anchors are a strict SUBSET of the lines the scanner hides — no arrangement of one constant makes those two sets equal, and this row does not claim it does. Neither caller accepts the opener alone: `parseAnchors` refuses an unterminated one with a diagnostic, and `matchesInFile` demands the same `-->` before it will hide anything. The literal carries no `g` flag, so no line's verdict depends on the line tested before it. **The ceiling, stated exactly rather than gestured at**: what still hides a line is any line carrying BOTH the opener and the three characters `-->` — presence anywhere on the line, not position after the opener. That is a fully forged anchor comment: inside a block comment, ahead of the code, or quoted in a string, and it need not be a well-formed anchor at all, because the scanner tests shape and never grammar. No test on one line's shape can close it: the fixtures under `test/verify/` quote whole anchors inside string literals and MUST stay hidden, and a forgery is byte-identical to them in shape. Requiring the terminator was measured before it was taken — it un-hid exactly 3 lines repo-wide and moved not one of the 528 legs' match sets. **Line-start is refused, and on the honest reason**: requiring the opener to begin the line un-hides 111 lines (61 under `test/verify/`, 34 under `test/anchor/`, 16 elsewhere), which measured today moves one leg's count — MV-40 `present`, 2 matches to 3 — and no leg's verdict, with `verify --strict` still at exit 0; so "false reports at scale" is what was feared, not what was measured. It is refused because it buys nothing against the case it targets and costs the reader: a forgery written AFTER the code stops hiding, a forgery written BEFORE it hides exactly as it does now, and the forger chooses the placement — while `parseAnchors` accepts an anchor anywhere on a non-indented line, so the scanner would begin scanning lines the reader still reads as law. **This row carries no tombstone, and the reason is the defect's own signature**: a leg forbidding the substring test in `brain:src/anchor/match.ts` was drafted, validated at 0 matches, then withdrawn on measurement — under the reverted code the offending line contains `@anchor` and hides itself from every leg, so the tombstone reads green in both states and can never fire. The five positive legs are what bite: reverting the scanner to the substring makes the `ANCHOR_LINE.test(lines[i])` leg find nothing, and dropping the terminator half makes the leg on `matchesInFile`'s `includes` call find nothing. | specified | active | 2026-08-17 | [changes/anchor-line-not-substring.md](changes/archive/anchor-line-not-substring.md) |
<!-- @anchor MV-82 brain:src/anchor/parse.ts /export const ANCHOR_LINE = \/<!--/ unique -->
<!-- @anchor MV-82 brain:src/anchor/parse.ts /!ANCHOR_LINE\.test\(raw\)/ -->
<!-- @anchor MV-82 brain:src/anchor/match.ts /ANCHOR_LINE\.test\(lines\[i\]\)/ -->
<!-- @anchor MV-82 brain:test/anchor/match.test.ts /a source line that mentions @anchor in a comment is scanned/ -->
<!-- @anchor MV-82 brain:src/anchor/match.ts /lines\[i\]\.includes\('--/ unique -->
<!-- @anchor MV-82 brain:test/anchor/match.test.ts /the terminator is required in any file type/ -->
| MV-83 | **The site serves its own type. Every typeface it uses is a file in this repository, served from the site's own origin, and no third-party font host is named anywhere in the site's sources.** The reason is MV-01: `verify`, `doctor` and `doors` make no network call, because a gate that fails for reasons unrelated to the code it judges gets bypassed. A documentation site that fetched its type from a CDN would contradict that on its own front page, and would disclose every reader's address to a party the reader never chose — while making the page's appearance depend on a host nobody here operates. Two faces, both SIL OFL 1.1 with the licence text shipped beside the files: **Archivo** carries human language and **Martian Mono** carries machine output, each one variable WOFF2 covering its whole weight and width range, Latin subset. The width axis is what separates the two voices — human widens as it climbs, machine sits narrow — which is why one file per voice covers display, body, chrome and code without a third family. The sources are absolute repo-local paths written as **literals in tracked files** rather than interpolated by a template, so a change that moves the fonts has to move this row's legs in the same commit. `static/` and not `assets/`: Hextra concatenates `custom.css` into its bundle under production and serves it at its own path otherwise, so a relative `url()` would resolve against two different directories and 404 in the mode nobody tests locally. The glob reaches **`CHANGELOG.md` as well as `site/**`**, because `site/hugo.yaml` mounts that one file in as a page (MV-78) and a leg stopping at the directory boundary would leave it unguarded; it **excludes `site/static/fonts/**`**, because scanning two compressed binaries as text on every pre-commit run buys nothing — a byte sequence inside a font table is not a request a browser will make. **The ceiling, stated rather than implied.** These legs prove that the sources in this repository name **the hosts they list** — the leg is a named list, not a universal, and an unnamed host would pass. They do **not** prove the built page issues no third-party request: `public/` is not tracked, and the theme is a Hugo module resolved at build time that could grow a remote reference in a version bump. No regex over a repo can see either. The browser check that does answer it — load a page with non-origin requests blocked and read the network log — is a step in the change's quickstart and belongs to the ritual, which multivac prints and never checks. **And what carries no leg at all**: that the result reads as two voices. A regex can confirm a token named `--font-machine` exists and that `code` references it; it cannot confirm the reader sees a distinction, and a leg pretending to would be exactly the failure this table exists to prevent. It is verified by eye, greyscaled, against the change's quickstart. The two cross-file facts are pinned by `test/invariants/site-fonts.test.ts` instead of by anchors, for the reason MV-72 and MV-77 already carry: **no anchor compares two trees** — that every `@font-face` source resolves to a path git actually tracks, and that every shipped font file has a licence file beside it, are both "for each X, a matching Y", which an anchor cannot say. | specified | active | 2026-08-17 | [changes/the-site-has-a-voice.md](changes/archive/the-site-has-a-voice.md) |
<!-- @anchor MV-83 brain:{site/**,CHANGELOG.md} !site/static/fonts/** /(fonts\.googleapis\.com|fonts\.gstatic\.com|use\.typekit\.net|fonts\.bunny\.net|fontawesome\.com|rsms\.me)/ absent -->
<!-- @anchor MV-83 brain:site/assets/css/custom.css /src: url\('\/fonts\/[a-z-]+\.woff2'\) format\('woff2'\)/ count=2 -->
<!-- @anchor MV-83 brain:site/layouts/_partials/custom/head-end.html /rel="preload" href="\/fonts\/[a-z-]+\.woff2"/ count=2 -->
<!-- @anchor MV-83 brain:site/assets/css/custom.css /--font-machine: 'Martian Mono'/ unique -->
<!-- @anchor MV-83 brain:test/invariants/site-fonts.test.ts /every @font-face source resolves to a tracked file/ -->
<!-- @anchor MV-83 brain:test/invariants/site-fonts.test.ts /every shipped font file has a licence beside it/ -->
| MV-84 | **The site's pages carry exactly ONE version string, and a test holds that one equal to the manifest.** Three rows already pin the versions this project writes down — MV-68 refuses a publish whose tag does not equal `package.json`, MV-77 holds the site's badge equal to it, MV-78 refuses a declared version with no dated changelog entry. All three hold **pinned** strings equal to each other. **None of them says anything about a version somebody typed into prose**, and prose is what a reader reads: `site/content/docs/guide/install.md` told every visitor that `mvac --version` prints `1.0.0`, that the package is `private: true`, and that it is unreleased — three statements, none ever true of a published multivac, sitting on the first page a stranger opens, under a law table that already carried 83 anchored rows. That is this tool's own failure mode committed by this tool: a claim nobody checked. The leg is `count=1` over `site/content/**`, measured at **3** before this change and **1** after. **Its scope is the site's pages, deliberately and not by oversight.** `DESIGN.md` and `CONTRIBUTING.md` state version numbers that are facts about other software and about a 2026-08-13 name placeholder; a rule reaching them would have to tell "a version this project currently is" from "a version something else once was", which is a judgement no regex makes. `CHANGELOG.md` is outside the glob **because of where it lives, not because it was excluded**: it sits at the repository root and `site/hugo.yaml` mounts it in at build time (MV-78), so `git ls-files site/content` never lists it and its dated headings stay legal without an exemption — a leg that depends on that mount, so moving the file moves this leg. **What this row does NOT say, and which check says it instead.** `count` is a **deletion ratchet, never a universal**: it counts across the whole file set together, so deleting the badge and writing a false version into `philosophy.md` leaves the count at 1 and passes — green, with the badge gone and the site lying. The half this leg could not state was stated by **MV-77's test**, which asserted the badge was present and equal to `package.json`. **Tightened 2026-08-17 to `absent`, and the ratchet argument above no longer applies.** MV-77's correction removed the last version literal from the site — the badge now renders a parameter the release pipeline sets from the last git tag — so the count is **zero**, and zero has no swap to be blind to: any version literal appearing under `site/content/` is a refusal, wherever it is. The test is deleted with the literal it compared. What is not written cannot drift, and this row is now the whole rule rather than half of one. | specified | active | 2026-08-17 | [changes/the-docs-say-what-ships.md](changes/archive/the-docs-say-what-ships.md) |
<!-- @anchor MV-84 brain:site/content/** /[0-9]+\.[0-9]+\.[0-9]+/ absent -->
| MV-85 | **A command REFUSES an argument it does not declare — flag or positional — before it does anything, and exits 2. It never ignores one.** **Amended 2026-08-18 by MV-104**: *declare* now means one object per command, the same one citty parses — and the refusal still runs BEFORE it. That order is the row, not a detail of it: measured on citty 0.2.2, `--nope` is parsed into a key nobody declared and the command runs having ignored it, which is this row's original defect wearing a dependency.  What this ended: `mvac doctor --sttrict` ran the report without the strict assertion and exited **0**. `--strict` exists in that command precisely to be an assertion rather than a description (MV-69 states its usage), so a typo silently downgraded an assertion to a report and a pipeline went green over a gate nobody had asserted was armed. That is this project's own failure mode — the tool claiming it checked, having checked nothing — committed by the tool. **Measured across the registry, four of nine were wrong**: `doctor` read `argv.includes('--strict')` and discarded the rest, `doors` took `_argv` and never looked at it, `seed` took the first non-flag and ignored every flag — all three exiting 0 — while `init` refused correctly but by throwing, which the dispatcher maps to 1. A fifth, `count`, exited 2 but printed only its usage block, never naming the argument it had not understood. **An earlier count of ONE wrong command was an artefact of the probe, not a property of the tool**: the shell loop used unquoted `$c`, and zsh does not word-split parameter expansions, so every iteration passed one string and measured *unknown command* nine times. The number in this row is from the re-measurement, and the wrong one is recorded here rather than quietly replaced. **Positionals are the same lie, and are covered for the same reason.** `doctor` declares `[--strict]` and no directory, then calls `doctorReport(ctx.cwd)`; `doors` declares no arguments at all. `mvac doctor /other/repo` therefore produced a truthful report about the working directory with nothing marking the substitution — worse than a refusal, because the reader has no way to notice. A rule about flags alone would have fixed the half that changes nothing and left the half that changes the whole answer. **Before any side effect, and that is measured rather than ordered.** `init`, `doors` and `seed` all write. A command that refuses after writing has still written, so the registry test asserts the scratch directory is untouched as well as the exit code — the assertion that makes this clause true instead of merely intended. **The row states BEHAVIOUR, not mechanism.** `src/lib/args.ts` holds one shared refusal because three of nine authors forgot; `verify` and `change` consume valued flags (`--repo <key>`, `--landed <repo>`) and keep their own correct loops, and a future command that hand-rolls a correct one satisfies this row. What holds the rule for the **tenth** command is not the helper but `test/cli/unknown-args.test.ts`, which walks the command registry rather than a list somebody typed — a list would be written by someone equally able to forget. No new dependency: an argument parser is what would be reached for here, and the law pins two runtime dependencies. **The ceiling, stated rather than implied**: this compares the command line against what a command DECLARES. A command that declares a flag and then ignores it passes every check here, and nothing in this row catches that — the declared surface is trusted, and only its enforcement is proven. | specified | active | 2026-08-17 | [changes/the-gate-cannot-be-typoed.md](changes/archive/the-gate-cannot-be-typoed.md) |
<!-- @anchor MV-85 brain:src/lib/args.ts /export function undeclared\(/ unique -->
<!-- @anchor MV-85 brain:src/commands/doctor.ts /undeclared\('doctor', argv, surfaceFrom\(ARGS\)\)/ unique -->
<!-- @anchor MV-85 brain:src/commands/doors.ts /const bad = undeclared\('doors', argv,/ unique -->
<!-- @anchor MV-85 brain:src/commands/seed.ts /undeclared\('seed', argv, surfaceFrom\(ARGS\)\)/ unique -->
<!-- @anchor MV-85 brain:src/commands/init.ts /class UsageError extends Error/ unique -->
<!-- @anchor MV-85 brain:src/commands/count.ts /count: unknown flag/ unique -->
<!-- @anchor MV-85 brain:test/cli/unknown-args.test.ts /for \(const c of commands\)/ unique -->
<!-- @anchor MV-85 brain:test/cli/unknown-args.test.ts /touched the tree before refusing/ -->
| MV-86 | **A brain records the version it was deliberately brought to, and a binary that disagrees SAYS SO on every run. It never refuses.** `npm i -g multivac@latest` updates the projector, not the projections: the doors, the skill tree, the hook shims and the harness settings in a brain were written by whatever binary ran `init` there, and no file recorded which. **This repository lived the consequence** — a global `mvac` ran 0.1.0 for weeks against a brain on 0.2.0, and when MV-82 was enacted that binary reported the row broken, because under its older scanner the line `export const ANCHOR_LINE = /<!--\s*@anchor\b/;` **hid itself**. The expensive half is not the stale projection but the SKEW: two people on different versions verifying the same brain do not disagree about speed, they disagree about what green means, and neither has a reason to suspect the other's binary. **Two fields, two owners, and neither writes the other's file.** `.multivac/projected.yml` carries `version:` and is tool-owned — a human never edits it. `.multivac/config.yml` carries `requires: ">=X.Y.Z"` and is hand-authored — **the tool never writes it**, because a floor is a decision and this tool does not answer for a human's decisions. The split is also mechanical: `init` is the only writer of `config.yml`, which is dense with hand-written comments a YAML round-trip would destroy, so a record living there would have made `doors` the first command to rewrite a file a person authors. **The record moves ONLY under an explicit act, and this is the decision the design turns on.** `mvac doors --adopt` re-projects and records; bare `mvac doors` re-projects and leaves the record alone. Restamping on every `doors` run was the first design and is wrong: people run `doors` after editing `doors:` or `grapher:`, and that would make the notice **vanish for a reason unrelated to the upgrade** — quiet, and looking resolved, which is worse than no notice. `verify` writes neither field ever: it runs inside somebody else's commit and inside hooks. **Nothing is refused.** Three severities — below a declared floor is red, a disagreeing or absent record is yellow, agreement is silent — and every one exits exactly as it would have. The precedent is the hook shim, which on finding no runnable multivac at all warns and exits 0: enforcement degrades, it never locks you out, and a wrong version is a near neighbour of absence rather than of malice. Every line names both versions and ends in the command that closes the gap, because a notice without an action is a nag and a nag is what people learn to scroll past. It is emitted **once, from the dispatcher** — nine call sites would be nine chances to forget, which is precisely how MV-85 happened — and the whole emission is guarded, because a notice must never take down the command it decorates. **The floor's grammar is a floor's grammar**: `>=X.Y.Z` and nothing else. `^0.3` or `>=0.3 <1` needs a semver RANGE parser, which is a third runtime dependency the law calls a design change, or a hand-rolled one, which is worse; every version this project has published is plain three-number semver, so the parser would exist to serve syntax nobody writes. A malformed floor is refused **by name** rather than ignored, since silently dropping it would be MV-85's defect relocated into a config file — the reader believing a gate is declared that is not. An **absent** record is an absence, never version zero: every brain alive today has none, and reading that as ancient would show the loudest notice to everybody on their first run. **The ceiling, stated rather than implied.** The record is PROVENANCE, not integrity: it says which version projected these files, never that they still are what was written. Hand-edit a door and the record stays exactly as fresh. And the notice reports a version GAP; it does not say what changed across it — that needs a per-release ledger of brain effects, declared by the release author and checked against its own diff, which is a later change and is not gestured at here as though it had shipped. | specified | active | 2026-08-17 | [changes/the-brain-knows-what-projected-it.md](changes/archive/the-brain-knows-what-projected-it.md) |
<!-- @anchor MV-86 brain:src/lib/version.ts /export function versionNotice\(/ unique -->
<!-- @anchor MV-86 brain:src/lib/version.ts /const FLOOR = \/\^>=/ unique -->
<!-- @anchor MV-86 brain:src/cli.ts /if \(n\) warn\(paint\(n\)\)/ unique -->
<!-- @anchor MV-86 brain:src/commands/doors.ts /const adopt = parseArgs\(argv, ARGS\)/ unique -->
<!-- @anchor MV-86 brain:src/commands/init.ts /await stamp\(dir\)/ unique -->
<!-- @anchor MV-86 brain:.multivac/projected.yml /^version: / unique -->
<!-- @anchor MV-86 brain:test/cli/version-skew.test.ts /the notice moved the exit code/ -->
<!-- @anchor MV-86 brain:test/cli/version-skew.test.ts /only init and doors --adopt may/ -->
| MV-87 | **A declared adapter is evaluated, acted on and reported PER ROOT**, where a root is the brain plus every declared repo present on disk (MV-56's search set), and **one root's artifact never answers for another's**. Measured in an ecosystem of six: a single sibling repo somebody had run `specify init` in by hand made `runScaffold` return before it scaffolded anything — the brain included — because presence was asked of the whole list and answered by the first hit; `doctor` printed `speckit: artifact ok` over five unequipped repos for the same reason, and the project-document pass accepted that one repo's constitution as the ecosystem's, so MV-76's gate passed on a document five repos had never seen. The rule is one sentence and it lands in four places: the scaffold runs the tool's own init in EVERY root that lacks the artifact and stays silent in every root that has it; the report prints one line per declared, present root, naming that root by the key the config gave it, the way `grapherLines` always has; the project-document gate asks each root where the tool is INSTALLED, refusing while any of them lacks a written document and naming each; and the grapher's first build reaches every declared, present repo rather than only the repos a change happened to touch — `refreshGraph` picks `create` where there is no artifact and `refresh` where there is, a distinction that already existed in doctor's wording and not in the runner. A repo opts out by declaring its own `sdd:`, the literal `none` meaning it has none — the per-repo override `grapher:` already had — and a root that resolves to no SDD is out of scope, not deficient: no scaffold, no gate, no notice. Nothing here moves a subprocess out of the change lifecycle (MV-01), and nothing derives a command from a tool's name (MV-59): a tool that declares no init still gets none, and the gap is stated once per root that lacks the artifact. **Amended 2026-08-18 by MV-90**: reaching every root is now REQUIRED, not merely attempted — `change close` refuses while a declared, present root has no graph. The refresh at close also stopped choosing its own scopes: it built that list by hand from the repos a change happened to name, a second enumeration of what `graphScopes` already returns, so a repo moved by another change, a merge or a sync was left describing a tree that was gone. It now calls `graphScopes` like everything else. **Amended 2026-08-18 by MV-93**: the door is a fifth per-repo surface, and it is the one that is NOT present-filtered — it is projected per DECLARED repo, on disk or not, because it renders from declarations and a door that changed with which repos happen to be cloned would differ between machines. The adapter RUNS stay present-filtered; only the rendering does not. | specified | active | 2026-08-17 | [changes/adapters-cascade-into-every-declared-repo.md](changes/archive/adapters-cascade-into-every-declared-repo.md) |
<!-- @anchor MV-87 brain:src/adapters/detect.ts /export const sddFor/ unique -->
<!-- @anchor MV-87 brain:src/adapters/detect.ts /export const NO_SDD/ unique -->
<!-- @anchor MV-87 brain:src/adapters/sdd.ts /Out of scope, not deficient/ unique -->
<!-- @anchor MV-87 brain:src/adapters/sdd.ts /installed here: silence, not a line/ unique -->
<!-- @anchor MV-87 brain:src/lib/config.ts /sdd: optString\(o\.sdd, `repos/ unique -->
<!-- @anchor MV-87 brain:test/change/sdd-gates.test.ts /not the first one that has it/ -->
<!-- @anchor MV-87 brain:test/change/sdd-gates.test.ts /does not decide the fate of the roots after it/ -->
<!-- @anchor MV-87 brain:test/doctor/adapters.test.ts /opts the repo out/ -->
<!-- @anchor MV-87 brain:src/commands/doctor.ts /The SDD, per root \(MV-87\)/ unique -->
<!-- @anchor MV-87 brain:src/commands/doctor.ts /out of scope, not a gap/ unique -->
<!-- @anchor MV-87 brain:test/doctor/doctor.test.ts /the sdd is reported per root/ -->
<!-- @anchor MV-87 brain:test/change/sdd-gates.test.ts /asks every installed root and names each that fails/ -->
<!-- @anchor MV-87 brain:src/adapters/refresh.ts /export async function ensureGraphs/ unique -->
<!-- @anchor MV-87 brain:src/adapters/refresh.ts /const first = !\(await artifactPresent\(spec, dir\)\)/ unique -->
<!-- @anchor MV-87 brain:src/commands/change.ts /await ensureGraphs\(brain, cfg\)/ count=2 -->
<!-- @anchor MV-87 brain:test/change/grapher-refresh.test.ts /no change has touched still gets its first graph/ -->
<!-- @anchor MV-87 brain:src/commands/{verify,doctor,doors}.ts /(ensureGraphs|runScaffold)/ absent -->
| MV-88 | **The published tarball carries no provenance attestation, and the pipeline says so where a reader meets it.** npm generates one by default under trusted publishing, and its registry refuses to verify any bundle produced outside a gitlab.com shared runner — measured on the `v0.6.0` tag, after the tarball was built, signed and written to the sigstore transparency log: `422 Unprocessable Entity — Error verifying sigstore provenance bundle: Unsupported GitLab CI runner environment: "self-hosted"`. This project's shared-runner minutes are exhausted and it runs on a self-hosted runner, so the choice was no releases, a long-lived token, or releases without provenance; the third is taken and `NPM_CONFIG_PROVENANCE` is `false` in the publish job, with the reason and the restore condition in the comment above it. **What this does NOT weaken is MV-68's claim**: the OIDC exchange succeeded from that same runner, so publishing is still trusted publishing and no long-lived token exists to leak — the limit is on the attestation, not on the credential, and `SIGSTORE_ID_TOKEN` is removed along with the attestation it minted. **The ceiling, stated rather than implied**: a tarball published from here carries no verifiable link back to the commit that built it, and no check in this repository can supply one — only the registry could, and this is the check it will not run. Restore it, dropping the variable and this row's mechanism, the day npm supports self-hosted runners or the day this project publishes from a shared runner again. | specified | active | 2026-08-18 | [changes/publish-carries-no-provenance-from-a-self-hosted-runner.md](changes/archive/publish-carries-no-provenance-from-a-self-hosted-runner.md) |

<!-- @anchor MV-88 brain:.gitlab-ci.yml /NPM_CONFIG_PROVENANCE: "false"/ unique -->
<!-- @anchor MV-88 brain:.gitlab-ci.yml /NO PROVENANCE, and the reason is not a preference/ unique -->
<!-- @anchor MV-88 brain:.gitlab-ci.yml /aud: sigstore/ absent -->
<!-- @anchor MV-88 brain:CHANGELOG.md /no provenance attestation/ -->
| MV-89 | **A change may exist before it starts, and starting it promotes the file that is already there.** `planned` is a change state beside `open` and `archived`, carried by the same file in the same directory with the same schema, plus a `horizon` of `now`, `next` or `later` — the whole ordering model, with no dates, no estimates, no rank and no dependencies between items. A roadmap kept anywhere else would be a second list describing the same work, and two lists drift within a week: whichever one the tool does not read becomes fiction. **Three properties, each load-bearing.** A planned change RESERVES NO INVARIANT ID — allocation stays at `change new`, because an id spent on work that never happens is a hole in the table no later change can fill. A planned change NEVER COUNTS AS UNCLOSED — `openChangeClaims` reads `open` alone, so it contributes neither a pending claim nor a landed repo and `finishedChanges` can never name it; had it counted, the first roadmap entry would have blocked every release for as long as the roadmap was not empty, which is forever. And the roadmap IS NEVER A GATE — no command refuses an operation because its subject was not recorded first, there is no flag to require it and no config key to turn it on, which is why this row carries an `absent` leg over `src/`: requiring a plan is unverifiable intent, the same category MV-27 keeps print-only. `change new` on a planned slug flips the status, reserves the id at that moment and keeps the body byte for byte, so intention and implementation are one document with one history; every later step — `plan`, `apply`, `land`, `close` — refuses a change that has not started and names `change new` as the step that comes first. **Deliberately not here, and the direction is fixed even though the mechanism is not**: these files are the SOURCE, and any projection of them — GitLab or GitHub issues, a board, labels — is a projection and never a second source. It flows one way: closing an issue in a forge closes nothing here, because a change closes at `change close` and nowhere else. The projection itself reaches the network, so it needs a declared adapter and lands as its own change; nothing in this row promises it exists. | specified | active | 2026-08-18 | [changes/roadmap-is-a-change-not-yet-started.md](changes/archive/roadmap-is-a-change-not-yet-started.md) |

<!-- @anchor MV-89 brain:src/change/file.ts /status: 'planned' \| 'open' \| 'archived'/ unique -->
<!-- @anchor MV-89 brain:src/change/file.ts /"status" must be "planned", "open" or "archived"/ unique -->
<!-- @anchor MV-89 brain:src/change/file.ts /export const HORIZONS = \['now', 'next', 'later'\]/ unique -->
<!-- @anchor MV-89 brain:src/change/file.ts /export function scaffoldPlanned/ unique -->
<!-- @anchor MV-89 brain:src/change/file.ts /export function assertStarted/ unique -->
<!-- @anchor MV-89 brain:src/commands/change.ts /assertStarted\(/ count=5 -->
<!-- @anchor MV-89 brain:src/commands/change.ts /promoted \$\{rel\} — planned since it was recorded/ unique -->
<!-- @anchor MV-89 brain:src/commands/verify.ts /`finishedChanges` can never name it/ unique -->
<!-- @anchor MV-89 brain:src/commands/roadmap.ts /no invariant id is reserved until it starts/ unique -->
<!-- @anchor MV-89 brain:src/commands/index.ts /  roadmap,/ unique -->
<!-- @anchor MV-89 brain:skills/multivac/references/change.md /planned — the state before the rhythm starts/ unique -->
<!-- @anchor MV-89 brain:skills/multivac/SKILL.md /PROMOTES the file rather than writing a second one/ unique -->
<!-- @anchor MV-89 brain:test/change/roadmap.test.ts /a planned change is invisible to the unclosed-change gate/ -->
<!-- @anchor MV-89 brain:src/**/*.ts /not (on|in) the roadmap/i absent -->
| MV-90 | **A declared grapher leaves a graph in every declared, present root, or `change close` refuses.** Declaring `grapher:` used to oblige nothing: `ensureGraphs` built where a root had none, `refreshGraph` refreshed at close, and every failure path was a notice that kept going — so a change could close with four declared repos ungraphed and say nothing. That is how the ecosystem this tool was measured against ended up with a declared grapher and five repos that never had a graph; MV-87 made the adapter REACH every root, it did not make reaching them REQUIRED. The cost is invisible by design, which is exactly why it needs a gate: the door tells every agent to ask the graph before reading the tree, so a missing graph does not fail — it degrades into agents grepping, which looks like working. **Build first, then judge**: the build-where-missing pass runs inside the gate, so the first close in a fresh ecosystem builds rather than refuses; a gate that refuses what it could have fixed teaches people to route around it. **Two verdicts refuse and two do not.** A root whose artifact is absent with the binary present refuses. A root whose binary is not on PATH ALSO refuses, naming the binary and the install hint — a gate that cannot be evaluated refuses rather than passes, which is what `sddGate` already does for the SDD validator. A root with no grapher applying to it is out of scope, and so is an UNVERIFIED adapter: demanding an artifact whose path would have to be guessed is Principle V's invented integration wearing a gate's clothes. **Amended 2026-08-18 by MV-103**: existence was half the question — an artifact that exists in one working tree answers this gate and helps nobody who clones the repository, where the door still tells every agent to ask a graph that is not there. The artifact must also be TRACKED, refused in a second gate that names the `git add` rather than running it. **The ceiling, stated rather than implied**: this gate asks whether a graph EXISTS and never whether it is CURRENT. Currency would have to be defined — mtime, content hash, tracked files newer than the artifact — and every definition is wrong on a fresh clone, where everything is newer than everything. A stale graph passes this gate, deliberately. **Two switches, in the SDD adapter's own words**: `--no-grapher` for one run, `grapher_auto: false` for good, both named on the line after any refusal, and a skipped gate says so out loud because silence about a skipped check is the failure this row exists to end. `close` only, and `--abandon` is exempt: an abandoned change made no claims and landed nothing, so demanding an artifact from it punishes dropping work. MV-01 keeps the gate out of `verify`, `doctor` and `doors`, which is an `absent` leg here, not a convention. **The door half**: the consumer door now carries the same graph block the brain's door has always carried, resolved with the grapher that applies to THAT repo, from one rendering rather than two — requiring an artifact in a repo whose own door never named it is the tool talking to itself. | specified | active | 2026-08-18 | [changes/the-graph-is-a-gate-not-a-suggestion.md](changes/archive/the-graph-is-a-gate-not-a-suggestion.md) |

<!-- @anchor MV-90 brain:src/adapters/refresh.ts /export async function graphGate/ unique -->
<!-- @anchor MV-90 brain:src/adapters/refresh.ts /Existence, never freshness/ unique -->
<!-- @anchor MV-90 brain:src/adapters/refresh.ts /or skip the gate without losing the tool/ unique -->
<!-- @anchor MV-90 brain:src/commands/change.ts /await graphGate\(brain, cfg, slug, noGrapher\)/ unique -->
<!-- @anchor MV-90 brain:src/commands/change.ts /'--no-grapher'/ unique -->
<!-- @anchor MV-90 brain:src/lib/config.ts /"grapher_auto" must be true or false/ unique -->
<!-- @anchor MV-90 brain:src/types.ts /grapherAuto: boolean/ unique -->
<!-- @anchor MV-90 brain:src/doors/consumer.ts /grapherLines\(config, entry\?\.grapher/ unique -->
<!-- @anchor MV-90 brain:src/commands/doors.ts /renderConsumerDoor\(config, key\)/ unique -->
<!-- @anchor MV-90 brain:test/change/grapher-gate.test.ts /close refuses while declared roots have no graph/ -->
<!-- @anchor MV-90 brain:src/commands/{verify,doctor,doors}.ts /graphGate/ absent -->
| MV-91 | **Re-running `init` never silently disagrees with the config it kept.** `init` resolved each adapter as `flag ?? config`, which is right on a first run — the flag is what writes the config a moment later — and wrong on a re-run, where the config is kept and the flag has already lost, except in the door written two lines further down. Measured, not reasoned about: `init --sdd speckit` then `init --sdd opsx` in the same repo left `sdd: speckit` in the config and `Features gate through the \`opsx\` SDD` in the door, with nothing saying they disagreed — and the door is the first file an agent reads. **A flag naming a different adapter from the one the config declares is REFUSED**, every disagreement in one refusal, naming the configured value, the requested value and both ways forward: change the config then run `doors`, or drop the flag. This is MV-85's shape — a command refuses what it cannot honour rather than proceeding as if you had not passed it — applied to the surface where it matters most. **The refusal writes NOTHING**, which is why the check sits immediately after the config is read and before the first create: placed beside the door it would leave a half-initialised repo behind. **Agreement is silent about being allowed and loud about being redundant**: a flag matching the config is reported as already declared, and a flag naming an adapter the config declares none of is reported with how to make it stick — never refused, because nothing disagrees, and never dropped in silence, because that is the defect. **The config is authoritative on a re-run, and the door projection reads it in that order** so the code states the rule rather than relying on the refusal above it. **Amended 2026-08-18 by MV-101**: *in that order* was written as `declared?.sdd ?? f.sdd` and called belt and braces, which is true only where the config DECLARES an adapter. Where it declares NONE the `??` fell through to the flag, so the row this same statement calls *reported, never refused, never dropped in silence* was silently honoured in the door — `init --sdd speckit` on a config naming no sdd reported the flag as not in it and wrote a door gating through speckit. The door now reads the config's answer whenever there is a config, and a flag reaches it exactly once: on the run that writes the config. **Amended 2026-08-18 by MV-102**: the leg that named that expression is gone with the expression — there is one door rendering now and it reads the config, so what the door names is anchored by MV-101 and MV-102. This row keeps what is its own: the refusal, the config read before the first write, and the report. **What was already correct and stays untouched**: config kept, law table, ritual, changes directory and `.gitignore` all write-if-missing, the door a managed block that leaves surrounding content alone, an older layout migrated rather than clobbered, and hooks installed without displacing the repo's own. A re-run has never appended twice or destroyed anything; the defect was narrow and it was about honesty. | specified | active | 2026-08-18 | [changes/init-says-what-it-ignored.md](changes/archive/init-says-what-it-ignored.md) |

<!-- @anchor MV-91 brain:src/commands/init.ts /init refused — \$\{CONFIG_PATH\} already declares/ unique -->
<!-- @anchor MV-91 brain:src/commands/init.ts /the config is authoritative on a re-run/ unique -->
<!-- @anchor MV-91 brain:src/commands/init.ts /const declared = await loadConfig\(dir\)/ unique -->
<!-- @anchor MV-91 brain:src/commands/init.ts /is not in it: add/ unique -->
<!-- @anchor MV-91 brain:test/init/reinit.test.ts /the door and the config can never name different adapters/ -->
| MV-92 | **A check runs the code in the tree it is checking.** Two mechanisms, one rule, both found by operating the tool rather than reading it. **The hook shim resolves the most specific multivac first**: the build in this repository, then the multivac this repository declares, then whatever is on PATH. The order was the exact inverse, and its comment stated it without justifying it — most likely because probing PATH is the cheapest test. Speed is the wrong axis: a repository that builds or declares a multivac has SAID which one governs it, while a global install is whatever the machine happens to have, including a laptop a year behind enforcing an older law table against a repo that pinned something else. Measured: committing in this repository ran a 0.5.0 install against a 0.7.0 brain, and MV-86's notice reported the mismatch that the runner choice had created. `findRunner` is the node mirror of the shell and the two are kept in step; a mirror that disagrees is worse than no mirror, because `doctor` reports from it. Nothing runnable still never blocks a commit, and the repository's own gates still run first with their exit code winning. **The build clears its compiled output before compiling.** `tsc` never deletes output for a source that has gone, so `dist-test/` accumulated whatever any branch ever compiled. Both directions are real: measured after a rebase, five failures from a file absent from that branch — and the silent inverse, a deleted test still reporting a pass, is a green nobody earned. **The property is pinned, not the script**: a test asserts that no compiled test exists without a source in the tree, so it fails whether the cause is a missing clean, a rename, or a build run some other way. The clean runs through the runtime rather than a shell `rm -rf`, so it works wherever the build works and adds no dependency. **The ceiling**: this row says a check runs the right code, never that the code is right. And the clean is a full rebuild — cheap here, and if it ever stops being cheap the answer is not to go back to output that outlives its source. | specified | active | 2026-08-18 | [changes/the-gate-runs-what-you-built.md](changes/archive/the-gate-runs-what-you-built.md) |

<!-- @anchor MV-92 brain:src/hooks/install.ts /MV-92: most specific first/ unique -->
<!-- @anchor MV-92 brain:src/hooks/install.ts /Runner order, most specific first/ unique -->
<!-- @anchor MV-92 brain:package.json /"prebuild":/ unique -->
<!-- @anchor MV-92 brain:test/init/hook-shim.test.ts /shim prefers the build in this repo/ unique -->
<!-- @anchor MV-92 brain:test/lib/build-output.test.ts /every compiled test has a source in the tree/ unique -->
<!-- @anchor MV-92 brain:package.json /rimraf/ absent -->
| MV-93 | **The door projected into a declared repo carries the ecosystem it belongs to.** It was four bullets — the law, the mount refresh, "the change may cross repos", and "run verify" — while the brain's door listed the repos and carried every adapter block. An operator entering through a code repo, which is the NORMAL case because code is where work happens, got an agent that never learned what else existed. **Three additions.** The ecosystem list names every declared repo with its path, marks the one whose door is being read, and carries a one-line `role` where the operator declared one — never derived, because what a repo is FOR is not in its path and `api — api` is worse than silence. It also names `brain` explicitly: that handle is an IMPLICIT anchor key, skipped when verify walks the declared repos and accepted as known regardless, so it can never appear in a list built from `repos:` — and a list promising "the keys anchors name" that omits the one key every consumer's anchors may use claims a completeness it does not have. No list at all below two declared repos: a heading over one row reading "(this repo)" is noise. **The adapters that apply HERE**, resolved per repo — the override first, the ecosystem's otherwise — from ONE rendering shared with the brain's door, never a second copy, which is the shape MV-90 set with the graph block and for the same reason: two renderings of one block is how the two disagree, and a door is where disagreement is least visible. **The mount refresh is the FIRST instruction**, not the second of four bullets, and carries its reason — the pin stays where the last commit left it, so a present mount is not a current one. It is the only line in that door with an ordering requirement. **Declarations, never disk**: a declared repo absent from this machine still appears, because a door that changed with which repos happen to be cloned would differ between two machines for reasons unrelated to the ecosystem — and the door is committed. The door makes no filesystem check and no network call, pinned by an `absent` leg narrowed to the CALL shape so a comment naming one of those functions cannot fail it. **The scaffolding clause is stated weakly on purpose**: the lifecycle runs the tool's own init where it is missing, or says why it could not. Naming one step would be wrong four ways — the scaffold runs from `new` and from the gate funnel covering plan, apply and close — and claiming it always scaffolds would be wrong three more, since no declared scaffold, an absent binary, and an init that exits without writing all report instead. A door that overstates the tool is Principle II broken in the file an agent reads first. | specified | active | 2026-08-18 | [changes/the-consumer-door-carries-the-ecosystem.md](changes/archive/the-consumer-door-carries-the-ecosystem.md) |

<!-- @anchor MV-93 brain:src/doors/consumer.ts /export function ecosystemLines/ unique -->
<!-- @anchor MV-93 brain:src/doors/consumer.ts /\*\*First, before reading anything in it:\*\*/ unique -->
<!-- @anchor MV-93 brain:src/doors/consumer.ts /the brain itself, mounted here at/ unique -->
<!-- @anchor MV-93 brain:src/lib/config.ts /role: oneLine\(optString\(o\.role, `repos/ unique -->
<!-- @anchor MV-93 brain:src/doors/brain.ts /export function sddLines/ unique -->
<!-- @anchor MV-93 brain:src/doors/brain.ts /runs the tool\\'s own init where it is missing, or says why it could not/ unique -->
<!-- @anchor MV-93 brain:test/doors/ecosystem.test.ts /the brain's handle is named/ -->
<!-- @anchor MV-93 brain:src/doors/consumer.ts /(existsSync|readFileSync|readdir|statSync)\(/ absent -->
| MV-94 | **A pin behind its channel is said at the moment work starts.** `stalenessLines` already computed it offline and `verify` already reported it, but `change new` and `change apply` — the two commands that mean "I am starting" — said nothing. "Refresh before you start" is exactly the instruction that has to arrive at the start; anywhere else it is a fact about the past. **It REPORTS and never refuses.** Offline, a pin behind its channel means somebody landed work OR nobody fetched, and those are indistinguishable from here — refusing on the second reading fails an ordinary morning, and a gate that fires on an ordinary morning is one people learn to skip. `staleness: block` still makes `verify` exit 1 exactly where it always did; this adds no second refusal, because two places to be wrong about one question is one too many. **One computation, exported rather than copied**, which is MV-90's lesson paid forward: `change close` kept its own scope list and the two disagreed by design. **The report runs BEFORE the bookkeeping commit**, deliberately: that commit moves the brain, so reporting afterwards would name a pin the tool itself had just put one behind — the operator is told the state they ARRIVED in, not the one the command created. Found by a test, not by reading. **The ceiling, stated rather than implied**: the read is offline, so it says what was last fetched, never what exists remotely. Every line carries how long ago this machine fetched, and a channel ref that does not resolve locally is reported as uncomparable rather than guessed. | specified | active | 2026-08-18 | [changes/a-stale-brain-is-said-at-the-moment-work-starts.md](changes/archive/a-stale-brain-is-said-at-the-moment-work-starts.md) |

<!-- @anchor MV-94 brain:src/commands/verify.ts /export async function stalenessLines/ unique -->
<!-- @anchor MV-94 brain:src/commands/change.ts /async function sayStaleMounts/ unique -->
<!-- @anchor MV-94 brain:src/commands/change.ts /await sayStaleMounts\(brain, cfg\)/ count=2 -->
<!-- @anchor MV-94 brain:src/commands/change.ts /BEFORE the bookkeeping commit below/ unique -->
<!-- @anchor MV-94 brain:test/change/staleness-at-start.test.ts /a stale pin never refuses/ -->
| MV-95 | **The tool says what it already computed, about working at once and about continuing — and gates neither, because neither can be checked.** **Parallelism.** `change apply` computes the landing plan and hands back one isolated checkout per repo, then said nothing about either. Repos in ONE stage of `landing_order` are the operator's own statement that they have no ordering dependency on each other — that is what putting them in a stage MEANS, so nothing is inferred — and the checkouts are the isolation that makes concurrent edits safe. `apply` now names them. Later stages are not named: they are blocked by an earlier one, which is the same declaration read the other way. **The boundaries ride with the line every time**, because they are its useful half: without them it reads "go faster", and both failures it would invite are quiet — two writers to one file is a lost update, and the law is a single table whose ids are allocated one at a time (MV-26), so changes serialise there by design. **Continuing.** The lifecycle already REFUSES to advance without each SDD step's artifact, so the sequence was never a choice — asking permission between steps costs a confirmation per step and decides nothing. Each printed step now carries the instruction to run the chain through, with the opt-out on the SAME line, which is the difference between a tool that assumes and a tool that decides for you. It names the distinction an agent cannot otherwise draw: a question the tool itself raises is not "may I continue", and an agent that conflates them either never stops or always stops. **Both halves are UNGATEABLE and declared so with their reason**, the way the registry already declares the SDD steps it cannot prove ran: no artifact proves an agent worked two repos at once, and none proves it did not stop to ask, so a gate on either would be a check reading what it cannot see. That is MV-27's reason, applied where it actually holds rather than as an excuse. | specified | active | 2026-08-18 | [changes/the-tool-urges-the-parallelism-it-already-knows-about.md](changes/archive/the-tool-urges-the-parallelism-it-already-knows-about.md) |

<!-- @anchor MV-95 brain:src/commands/change.ts /are one stage: no ordering between them/ unique -->
<!-- @anchor MV-95 brain:src/commands/change.ts /never the same file twice at once/ unique -->
<!-- @anchor MV-95 brain:src/adapters/sdd.ts /run the chain through without asking to continue/ unique -->
<!-- @anchor MV-95 brain:test/change/urging.test.ts /the boundaries ride with the line, every time/ -->
| MV-96 | **A derived page states what this ecosystem's declarations oblige, sorted into automatic, gate and yours — and binds nothing.** A newly initialised brain had no single place answering "what did I just sign up for": the law is dozens of rows, the ritual starts empty, and the obligations were spread across an adapter registry the operator never reads and command output that scrolls past. The reasonable conclusion from reading a fresh repo was that nothing is enforced, which is false and was the tool's fault. `doors` writes `.multivac/flow.md` through the managed block, so it is rewritten whole every projection while anything written outside the markers survives. **Derived, never authored** — the ritual is the operator's and is never overwritten; this is the tool's and always is, and keeping that line sharp is what stops it becoming a second law table. **Every row is RENDERED from the registry and the config**, the same data the gates read, so it cannot describe behaviour the tool does not have: a row typed ABOUT a gate can drift, a row rendered FROM it cannot. A gate row leads with the command that refuses and names the artifact, both taken from the step's own fields. An unprovable step carries the adapter's own reason VERBATIM, because a paraphrase would age beside its source — the failure this project exists to prevent. An unverified adapter is named as declared-but-unknown with nothing invented. **No invariant identifier ever appears in the generated text**, and this is the row's sharpest clause because it killed the feature's first design: ids are allocated from each brain's OWN table and `init` writes a table with zero rows, so a generated `MV-56` names a different rule — or none — in every ecosystem except the one it was written in. An `absent` leg keeps it out, because the temptation to add one for authority is exactly what a later reader will feel. **The ceiling**: this page binds nothing and says so in its own header. The law binds; this describes what the law and the declared adapters already do, for a reader who has not read the table. | specified | active | 2026-08-18 | [changes/the-flow-is-derived-from-what-was-declared.md](changes/archive/the-flow-is-derived-from-what-was-declared.md) |

<!-- @anchor MV-96 brain:src/doors/flow.ts /export function renderFlow/ unique -->
<!-- @anchor MV-96 brain:src/doors/flow.ts /\*\*This page binds nothing\*\*/ unique -->
<!-- @anchor MV-96 brain:src/doors/flow.ts /MV-[[:digit:]]+`/ absent -->
<!-- @anchor MV-96 brain:src/commands/doors.ts /renderFlow\(config\)/ unique -->
<!-- @anchor MV-96 brain:src/lib/config.ts /export const FLOW_PATH/ unique -->
<!-- @anchor MV-96 brain:test/doors/flow.test.ts /the page carries no invariant identifier/ -->
| MV-97 | **Modifying the declared config needs an open change; creating one is free.** `.multivac/config.yml` decides which repos exist, which adapters bind, which channel is read and which gates run — every one of those as load-bearing as a law row, and all of them editable in a commit with no explanation and nobody noticing: a repo dropped from `repos:` stops being verified, an adapter removed stops obliging anything. The check sits beside MV-81's, is built the same way — read the index, compare against HEAD, decide — and it GATES, because a reported-only version would be a line nobody acts on, which is precisely what the grapher was before MV-90 gave it teeth. **Created is free, and that is the whole exemption story.** Exactly one code path writes this file — `init`, and only when it is absent — so the rule reads what the commit DOES rather than who claims to have done it, and there is nothing to forge. A brain has to be able to be born, and a rule that blocks initialisation is a rule nobody can adopt. **The weak reading, deliberately: ANY open change satisfies it.** The strong one — a change that NAMES the config — would need a field the change file does not have, and adding one to make this check stronger would be a schema change in service of a check rather than of the work. What the weak reading buys is that the edit lands on a branch with a merge request describing it, which is where a human reads it. **Open changes are read from the directory, not from the pendency map**: that map holds only changes DECLARING a claim, and a change opened to make this very edit has none yet — the common case, which would have made the rule unusable. **The index, never the working tree**: the index is what is about to be committed; a working-tree read would refuse a commit for an edit deliberately left unstaged, and miss a staged edit since reverted from the file. An unreadable index is reported unanswered rather than assumed clean. **The ceiling**: this proves an edit happened inside a lifecycle. It never proves the change is ABOUT the config, and never that the edit is wise. | specified | active | 2026-08-18 | [changes/the-declared-config-is-itself-invariant.md](changes/archive/the-declared-config-is-itself-invariant.md) |

<!-- @anchor MV-97 brain:src/commands/verify.ts /async function configLine/ unique -->
<!-- @anchor MV-97 brain:src/commands/verify.ts /is modified and no change is open/ unique -->
<!-- @anchor MV-97 brain:src/commands/verify.ts /function openChangeSlugs/ unique -->
<!-- @anchor MV-97 brain:src/commands/verify.ts /creating one is free; a brain has to start somewhere/ unique -->
<!-- @anchor MV-97 brain:test/verify/config-gate.test.ts /including one opened for this very edit/ -->
| MV-98 | **A fresh brain gets a ritual seeded from what it declared, every line commented — and a ritual line a check could make true belongs in the check.** `init` wrote the ritual as a bare comment, and facing a blank page most people write nothing, so `change close` printed nothing forever. It now carries candidates drawn from the declarations, **all commented out**: an unadopted ceremony is not a ceremony, and writing obligations into an operator's file would be the tool deciding what their team owes each other. The idiom is the one `init` already uses for detected adapters in the config it writes — suggested, commented, enabled by a human. A fresh brain therefore still prints NOTHING at close, because the checklist strips comment blocks, so nothing changes until somebody means it. **Only what no check could decide is seeded.** A declared grapher contributes none: its work is automatic and MV-90 already requires its artifact, so seeding it would move a checked thing onto a poster — the exact inversion this row exists to undo. **And this brain's own ritual was that inversion.** All four of its lines were checkable and none was checked. Three moved: the pushed-branch line is already refused by `change close` until every declared repo is recorded landed; the claims-and-order line is prompted by the merge request template on disk, and MV-34 is amended here to anchor BOTH halves rather than the claims alone; and the friction line is superseded by the roadmap MV-89 built. What is left is what nothing can decide. **The fourth is recorded as still checkable and deliberately NOT checked**: "the site says what the tool now does" needs a diff against the fork point, because at close the branch is already an ancestor of the trunk, so a merge-base diff is EMPTY BY CONSTRUCTION and the gate would have refused every correctly landed change. That mechanism is worth building and is not this row's; pretending otherwise in the meantime is what the row forbids. **The ritual stays authored and is never overwritten** — the opposite of MV-96's flow page, which is derived and rewritten whole. That line is what keeps the tool out of the operator's ceremony. | specified | active | 2026-08-18 | [changes/the-ritual-is-seeded-and-its-checkable-lines-become-law.md](changes/archive/the-ritual-is-seeded-and-its-checkable-lines-become-law.md) |

<!-- @anchor MV-98 brain:src/lib/ritual.ts /export function ritualSeed/ unique -->
<!-- @anchor MV-98 brain:src/lib/ritual.ts /an unadopted ceremony is not a ceremony/ unique -->
<!-- @anchor MV-98 brain:src/commands/init.ts /ritualSeed\(declared \?\? f\)/ unique -->
<!-- @anchor MV-98 brain:test/change/ritual.test.ts /keeps only what no check could decide/ -->
| MV-99 | **A declared tracker receives the roadmap as a ONE-WAY projection.** The change files are the source; issues are their projection and never a second source, because a projection that reads back is two lists again — the failure the roadmap was built to end, moved one layer out. Closing an issue by hand closes nothing: the next `roadmap sync` restores it, because the change file said so, and nothing the tracker returns ever reaches a change file. **The identity is the NUMBER recorded in the change file's frontmatter.** It survives a title edit, which is what breaks the alternative — searching the tracker for a matching title, the way the SDD tool's own issue command does, which duplicates everything the day somebody renames one. Not a URL: the project comes from the repo's remote, so a link carries a derivable half that would have to be parsed back out on every run and goes stale the day the repo moves. **The vendor's own CLI does the talking** — `glab`, `gh` — because it already solves authentication, hosts and enterprise installs; a client written here would add a dependency to do the same job worse and would put credential handling in a tool whose whole claim is that it holds none. A tracker absent from the registry is UNVERIFIED and gets no entry rather than a guessed one. **Only multivac's own label namespace is written**, and no label it does not own is ever removed: teams label for their own reasons, and one wiped triage is enough to have the projection turned off permanently — after which the drift it prevented comes straight back. **An absent binary REFUSES**: a projection that cannot run must not report success. A recorded number whose issue is gone is reported and never silently re-created, because that is how a change ends up with two issues and nobody can tell which one people commented on. **It reaches the network, so it runs from `roadmap sync` and nowhere else** — never from `verify`, `doctor` or `doors`, which MV-01 keeps offline, and an `absent` leg keeps that true rather than remembered. **Scope, stated rather than implied**: one issue per CHANGE. A story-level projection is the intent and is not built here — it needs a second reader of the SDD tool's task list, a file with exactly one consumer today, and reading somebody else's artifact from a second place is a coupling worth its own change. | specified | active | 2026-08-18 | [changes/the-roadmap-projects-to-a-declared-tracker.md](changes/archive/the-roadmap-projects-to-a-declared-tracker.md) |

<!-- @anchor MV-99 brain:src/adapters/tracker.ts /export const LABEL_PREFIX/ unique -->
<!-- @anchor MV-99 brain:src/adapters/tracker.ts /never removes one it does not own/ unique -->
<!-- @anchor MV-99 brain:src/adapters/tracker.ts /export function notInstalled/ unique -->
<!-- @anchor MV-99 brain:src/commands/roadmap.ts /async function sync\(brain: string, cfg: Config\)/ unique -->
<!-- @anchor MV-99 brain:src/commands/roadmap.ts /not re-created; clear/ unique -->
<!-- @anchor MV-99 brain:src/change/file.ts /The NUMBER, not a link/ unique -->
<!-- @anchor MV-99 brain:src/lib/config.ts /const tracker = optString\(o\.tracker/ unique -->
<!-- @anchor MV-99 brain:test/change/tracker.test.ts /a second run creates nothing/ -->
<!-- @anchor MV-99 brain:src/commands/{verify,doctor,doors}.ts /tracker\.js/ absent -->
| MV-100 | **Every page of the site carries a description and a card image, so a pasted link renders as itself.** Measured on the deployed site: `<meta name=description>` was EMPTY, `og:type` was whitespace, and `og:image` appeared ZERO times on any page — documentation pages carried `twitter:title` and no `og:` tags at all. A link pasted into a social surface came out bare, and that is not the surface being difficult: a scraper that finds no image does not fall back to a favicon or to a logo in the page, it renders a link. The missing image is why there was no card; the empty description only made it worse. **No template was written**, because the machinery was already there and had never been given anything to render: the theme emits `og:image` from `params.images` and emits none without it, and Hugo's own card partial picks the large format the moment an image exists — so the format follows from the same declaration instead of being stated twice, where two statements would eventually disagree. **Descriptions are AUTHORED per page**, with the site's own as the fallback. The theme otherwise derives one from whatever prose comes first — a sentence written to open a section, not to describe a page — and a description nobody wrote is a description nobody checked. **The card is committed, not generated**: a rasteriser is not a build dependency this project has, and acquiring one to draw a logo is the third dependency arriving through a side door. **It is wordless on purpose**: the lockup renders its wordmark through a system font stack, so rasterising it would bake in whichever fonts the drawing machine happened to have — the same differs-by-machine this tool refuses everywhere else — and the site's own faces ship as woff2, which no rasteriser here reads without a converter and a compression library. The title and description travel beside the image as text, where a reader can read them and an author can correct them. **The crawler file names the sitemap**, which already returned 200 while nothing pointed at it. **The rest of the discoverability surface, since a card is not SEO.** `enableGitInfo` gives every sitemap entry a `lastmod` from git rather than from a date field nobody would maintain. `disableKinds` stops generating `/categories/` and `/tags/`, which were in the sitemap with no content on them — thin pages in an index are a cost, not an omission. `languageCode` replaces a `locale:` key that Hugo does not read: an inert line that LOOKED like a setting, which is worse than an absent one. And the head gains what the theme does not emit and only that — `og:site_name`, `og:locale`, the card's dimensions and alt text, and JSON-LD naming what KIND of thing each page is, since the theme's microdata can say a name and a description but not that this is a developer tool rather than a shop. Every added tag is additive: a duplicate is two sources for one fact, and the day they disagree the wrong one may win. **One gap left open on purpose, with its price**: the home page's `<title>` is the site title alone, because the theme composes it that way and offers no hook. Fixing it means shadowing an eighty-line theme partial that will drift on the next upgrade — a worse trade than the four words are worth. Every other page already reads `X – multivac`, and the home's pitch now lives in its description and its structured data. **THE STANDING RULE, and the half with teeth**: the site's discoverability surface is not a thing that was fixed once — it is checked on every commit, and a change to the site that removes it FAILS. The check walks the content tree RECURSIVELY rather than reading a list, because a listed set passes forever while the section somebody adds next week ships with no description at all: that silent regression is the thing this row exists to catch, not the state of any particular day. Every section landing authors its own description, the site declares a fallback so no page can be description-less, the card image is declared and is a raster of the size scrapers read, the crawler file names the sitemap, and the origin is absolute so every canonical resolves. **Two ceilings, stated rather than implied**: this cannot decide whether any given network renders the card — that is their scraper's call — and it cannot decide whether a description is any GOOD, only that somebody wrote one. The check reads the DECLARATIONS rather than a built site, because the test job runs a node image with no site builder in it; a check that quietly weakened depending on its environment would be worse than one that says what it checks. | specified | active | 2026-08-18 | [changes/the-site-can-be-shared.md](changes/archive/the-site-can-be-shared.md) |

<!-- @anchor MV-100 brain:site/hugo.yaml /^  images:/ unique -->
<!-- @anchor MV-100 brain:site/hugo.yaml /^enableRobotsTXT: true/ unique -->
<!-- @anchor MV-100 brain:site/hugo.yaml /a scraper that finds no image does not fall back/ unique -->
<!-- @anchor MV-100 brain:site/layouts/robots.txt /Sitemap:/ unique -->
<!-- @anchor MV-100 brain:site/hugo.yaml /^enableGitInfo: true/ unique -->
<!-- @anchor MV-100 brain:site/hugo.yaml /^disableKinds:/ unique -->
<!-- @anchor MV-100 brain:site/hugo.yaml /^languageCode:/ unique -->
<!-- @anchor MV-100 brain:site/hugo.yaml /^locale:/ absent -->
<!-- @anchor MV-100 brain:site/layouts/_partials/custom/head-end.html /application\/ld\+json/ unique -->
<!-- @anchor MV-100 brain:site/layouts/_partials/custom/head-end.html /jsonify \| safeJS/ unique -->
<!-- @anchor MV-100 brain:test/site/cards.test.ts /the dimensions link scrapers expect/ -->
<!-- @anchor MV-100 brain:test/site/cards.test.ts /including one added tomorrow/ -->
| MV-101 | **The door names an adapter only where the config declares one, so `init` and `doors` never disagree about which tool gates the work.** MV-91 ended the case where a flag DISAGREED with the config and left one row of its own table open: a flag naming an adapter the config declares none of is *reported with how to make it stick, never refused, never dropped in silence* — and then it was dropped everywhere except the door. Measured on 0.7.0: `init --quiet .` (no adapter flag, so the config declares none), then `init --sdd speckit .` printed *`--sdd speckit` is not in it: add `sdd: speckit` there* and wrote `Features gate through the \`speckit\` SDD` into AGENTS.md. The run said the flag did not take effect and wrote the file where it did. **The second command is what makes it visible**: `doors` reads the config alone, so the next projection DELETED the block — two commands, one repo, two doors, and which one an agent reads depends on which ran last. That is worse than the disagreement MV-91 caught, because nothing on disk records the choice: the config is empty, so the revert has no diff that explains it and no run ever mentions it. **One operator, not a new rule**: `declared?.sdd ?? f.sdd` becomes the config's answer whenever a config EXISTS — where it exists but will not parse, or exists and names nothing, the door names nothing. A flag is the declaration exactly once, on the first run, where there is no config to read and the flag is what writes it. The report does not move: MV-91 already says why this case is reported and not refused. **Amended 2026-08-18 by MV-102**: the ceiling this row stated — that the two commands still wrote different door BODIES — is closed, and with it the expression this row first anchored. There is one rendering now, read from the config, so the rule holds without a second place to state it. | specified | active | 2026-08-18 | [changes/the-door-says-only-what-the-config-declares.md](changes/archive/the-door-says-only-what-the-config-declares.md) |
<!-- @anchor MV-101 brain:src/commands/init.ts /const cfg = await loadConfig\(dir\)/ unique -->
<!-- @anchor MV-101 brain:src/commands/init.ts /a flag reaches it exactly once/ unique -->
<!-- @anchor MV-101 brain:src/commands/init.ts /adapters the CONFIG declares/ unique -->
<!-- @anchor MV-101 brain:src/commands/init.ts /declared\?\.sdd \?\? f\.sdd/ absent -->
<!-- @anchor MV-101 brain:test/init/reinit.test.ts /init and doors name the same adapter/ -->
| MV-102 | **The brain door has ONE rendering, and `init` writes the bytes `doors` writes.** `init` carried its own copy of the door — a template literal written out by hand, plus its own assembly of the SDD lines — while `doors` built the same document from the config through `renderBrainDoor`. Two renderings of one document, and the copy drifted in the direction that costs most: `renderBrainDoor` gained the graph block (MV-90) and the ecosystem's repo list (MV-93); the copy gained neither. So the door a fresh brain got — the FIRST file an agent reads, and the only one it has before anybody runs a second command — never named the graph the door itself tells you to ask before reading the tree. Measured: `mvac init --grapher graphify .` left a door with zero mentions of graphify, and `mvac doors`, run next with nothing edited, replaced the whole managed block. **The fix is a deletion**, not a third helper: `init` calls the function that already renders this document. The rule the deletion buys is that a change to the brain door's wording is made in exactly one place — the state where two copies exist is the state that produced this row. **A config that will not load leaves the door alone**: writing one from nothing would be `init` inventing the brain it is scaffolding, so the config's own error stands and the door waits for `doors`. **This is also where MV-101 now lives**: the door names the adapters the config declares because the rendering reads the config, not because a second expression says so. | specified | active | 2026-08-18 | [changes/the-brain-door-has-one-rendering.md](changes/archive/the-brain-door-has-one-rendering.md) |
<!-- @anchor MV-102 brain:src/commands/init.ts /ONE rendering, and it is/ unique -->
<!-- @anchor MV-102 brain:src/commands/init.ts /renderBrainDoor\(cfg, countActiveInvariants/ unique -->
<!-- @anchor MV-102 brain:src/commands/init.ts /DOOR_BODY/ absent -->
<!-- @anchor MV-102 brain:test/init/reinit.test.ts /init and doors write the same door, byte for byte/ -->
<!-- @anchor MV-102 brain:test/init/init.test.ts /the scaffolded door names the declared grapher/ -->
| MV-103 | **A declared grapher's artifact is part of the REPOSITORY it describes, not part of one checkout of it: `change close` refuses while a declared, present root keeps its graph untracked or ignored.** MV-90 made a declared grapher oblige a graph and asked one question about it — does the artifact exist on disk. A graph that exists only in the author's working tree answers that question and helps nobody else: the next clone has none, every gate there passes only because the build-where-missing pass rebuilds it, and until somebody runs the tool the door in that clone still tells every agent to ask a graph that is not present. Declaring a grapher is a declaration about the repository. **Two ways to fail, two messages, because the fixes differ**: untracked is one `git add` away, and ignored by a rule is not — `git add` on an ignored path reports nothing an author is likely to read, and `-f` is the wrong advice when the RULE is what is wrong, so that case names the rule and says to remove it first. **multivac still stages nothing.** MV-50 keeps the refresh module out of git entirely — a refresher that touches the index turns a background convenience into something that edits your commit — and this row does not relax it: the gate refuses and prints the command, the human runs it. That is also why the check lives in `src/adapters/tracked.ts` and not beside the refresh it extends: MV-50 carries an `absent` leg over `refresh.ts` for `git`, and the file layout is what states that boundary to the next reader. **A missing artifact stays MV-90's refusal** and is silent here — two refusals for one root, one derived from the other, is noise. **The obligation is the declared ARTIFACT, not the directory around it**: the tool writes caches, dated exports and generated HTML beside it, which every user's own ignore rules exclude, so demanding the whole directory would be a rule its author already breaks. Out-of-scope roots, unverified adapters, both skip switches and `--abandon`'s exemption are MV-90's and are unchanged. `doctor` reports the same state per root and gates on nothing. | specified | active | 2026-08-18 | [changes/the-graph-is-part-of-the-repository.md](changes/archive/the-graph-is-part-of-the-repository.md) |
<!-- @anchor MV-103 brain:src/adapters/tracked.ts /export async function graphTrackedGate/ unique -->
<!-- @anchor MV-103 brain:src/adapters/tracked.ts /is ignored by \.gitignore — remove the rule/ unique -->
<!-- @anchor MV-103 brain:src/adapters/tracked.ts /Nothing in this file writes/ unique -->
<!-- @anchor MV-103 brain:src/commands/change.ts /await graphTrackedGate\(brain, cfg, slug, noGrapher\)/ unique -->
<!-- @anchor MV-103 brain:src/lib/git.ts /export async function isTracked/ unique -->
<!-- @anchor MV-103 brain:src/commands/doctor.ts /UNTRACKED →/ unique -->
<!-- @anchor MV-103 brain:test/change/grapher-tracked.test.ts /the gate stages nothing/ -->
<!-- @anchor MV-103 brain:test/change/grapher-tracked.test.ts /close proceeds once the graph is tracked/ -->
<!-- @anchor MV-103 brain:src/adapters/refresh.ts /isTracked|graphTrackedGate/ absent -->
| MV-104 | **Every command declares its arguments ONCE, as data, and that one declaration is both what citty parses and what the refusal reads — parsing is a dependency, refusing what was not declared is not.** Ten commands hand-rolled ten argument loops, and each stated its surface twice: once for `undeclared`, once in the loop that parsed it. The two could drift and nothing noticed. The declaration is now a citty `ArgsDef` and `surfaceFrom` derives the surface from it, so adding a flag is one edit. **The refusal is NOT delegated**, and the reason is measured rather than assumed: citty 0.2.2 parses `--nope` into a key nobody declared and hands it to the command, which ignores it and runs; extra positionals arrive the same way, bound to nothing. That is MV-85's defect exactly — the row written because `doctor --sttrict` ran a non-strict report and exited 0 — so `undeclared` runs first, and a parser that never sees an undeclared argument cannot silently drop one. **The exits stay the tool's**: citty throws its own `CLIError` for a missing `required` positional, so no positional is declared required — `count` answers a missing spec with its usage block and exit 2, as it always has. **Two flags are read literally**, not through the parser: citty owns the `--no-` prefix for negation, so `--no-sdd` arrives as `sdd: false` and the flag `change` declares never appears. The declaration still lists them, because the refusal and the usage are about the surface a user types. **What was not adopted**: citty's `--help`. MV-69 has every command declaring its own usage, the site documents that exact output, and a generated table says less than the prose these commands carry. The dispatcher still answers `--help` before any command runs. **The cost, in the open**: a third runtime dependency, which MV-02 pinned at two and the constitution named — both amended here, dated, before the package was added. | specified | active | 2026-08-18 | [changes/the-cli-is-parsed-by-citty.md](changes/archive/the-cli-is-parsed-by-citty.md) |
<!-- @anchor MV-104 brain:src/lib/args.ts /export function surfaceFrom/ unique -->
<!-- @anchor MV-104 brain:src/lib/args.ts /One declaration, two readers/ unique -->
<!-- @anchor MV-104 brain:src/commands/verify.ts /satisfies ArgsDef/ unique -->
<!-- @anchor MV-104 brain:src/commands/change.ts /citty owns the `--no-` prefix/ unique -->
<!-- @anchor MV-104 brain:src/commands/count.ts /citty THROWS a CLIError/ unique -->
<!-- @anchor MV-104 brain:package.json /"citty": "/ unique -->
<!-- @anchor MV-104 brain:test/cli/args.test.ts /the refusal runs before the parser/ -->
| MV-105 | RESERVED by change the-refusal-reads-the-whole-token — state the rule here before close. | open | proposed | 2026-08-18 | [changes/the-refusal-reads-the-whole-token.md](changes/the-refusal-reads-the-whole-token.md) |
| MV-106 | RESERVED by change the-gate-reads-the-commit-it-gates — state the rule here before close. | open | proposed | 2026-08-18 | [changes/the-gate-reads-the-commit-it-gates.md](changes/the-gate-reads-the-commit-it-gates.md) |
| MV-107 | RESERVED by change the-gate-reads-the-commit-it-gates — state the rule here before close. | open | proposed | 2026-08-19 | [changes/the-gate-reads-the-commit-it-gates.md](changes/the-gate-reads-the-commit-it-gates.md) |
| MV-108 | RESERVED by change presence-is-not-identity — state the rule here before close. | open | proposed | 2026-08-19 | [changes/presence-is-not-identity.md](changes/presence-is-not-identity.md) |
| MV-109 | RESERVED by change the-engine-reads-one-way — state the rule here before close. | open | proposed | 2026-08-19 | [changes/the-engine-reads-one-way.md](changes/the-engine-reads-one-way.md) |
| MV-110 | RESERVED by change the-ledger-keeps-itself — state the rule here before close. | open | proposed | 2026-08-19 | [changes/the-ledger-keeps-itself.md](changes/the-ledger-keeps-itself.md) |
| MV-111 | RESERVED by change a-paraphrase-ages-silently — state the rule here before close. | open | proposed | 2026-08-19 | [changes/a-paraphrase-ages-silently.md](changes/a-paraphrase-ages-silently.md) |
