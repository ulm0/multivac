---
slug: constitution-state
status: archived
repos:
  brain:
    status: landed
landing_order:
  - - brain
invariants:
  touches: []
  adds:
    - MV-135
  retires: []
claims:
  - id: MV-135
    statement: a project document's state is one verdict, template meaning the tool's recorded template or one of its own tokens outside HTML comments; change new asks for an unwritten one without urging; flow.md shows its gate; openspec's context is reported and never gated; and an active row outranks any project document.
---

# The project document's state is read one way, asked for without urging, and outranked by the law

The template test refused written constitutions that cite `[1]`, `change new` never asked for the document the next command refuses on, and nothing said a row outranks it. See `specs/060-constitution-state/`.
