# Implementation Plan: session-zero-equips

## Summary
- **`init.ts`.** Rewrite the closing steps. The flow that fits is marked, not chosen.
- **`seed.ts`.** Add a `### setup` section per present repo, using the grapher state and the project-document verdict. The third question names the written documents.
- **Skill and references.** Reorder the steps, then copy them to `.claude/skills`.
- **Site.** Update getting-started, commands (`init`, `seed`) and session-zero.

## Constitution Check
- **I.** New row MV-136.
- **IV.** `seed` stays offline: files and git only.
