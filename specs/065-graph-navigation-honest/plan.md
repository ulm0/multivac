# Implementation Plan: graph-navigation-honest

## Summary
- **`brain.ts`.** `grapherLines` words the refresh by `postEditDoors(config)`. It names the verbs in one line and cites the vendor section where `spec.harness` covers a declared door.
- **`settings.ts`.** `refreshHookCmd(refresh, env, artifact?)` resolves the edited file's toplevel when that repo holds the artifact. `doors` passes the artifact.
- **`flow.ts`.** Fix the grapher auto and gate lines, and add a "yours" navigation line.
- **`doctor.ts`.** Add a navigation line and a missing-section line.

## Constitution Check
- **I.** New row MV-140, with notes on MV-52 and MV-61.
