# Contract: `.multivac/flow.md`

Written by `doors` (and by `init`, which calls it). Managed block; anything an
operator writes outside it survives.

```markdown
<!-- multivac:begin -->
# Flow — what this ecosystem's declarations oblige

Generated by `multivac doors` from `.multivac/config.yml` and the adapter
registry. Edits inside these markers are overwritten. **This page binds
nothing** — the law in `.multivac/invariants.md` binds, and this describes what
it and the declared adapters already do.

## Automatic — multivac does it, you do not ask

- the code graph is built where a declared root has none, and refreshed at `change close`, in every declared repo
- the doors are re-projected by `multivac doors`, and the SDD tool's own init is run where its artifact is missing

## Gate — multivac refuses without it

- `change plan` refuses without `specs/*<slug>*/spec.md`
- `change apply` refuses without `specs/*<slug>*/plan.md`
- `change apply` refuses without `specs/*<slug>*/tasks.md`
- `change close` refuses while a declared, present repo has no `graphify-out/graph.json`
- `change close` refuses while a declared claim does not resolve

## Yours — nobody can check these

- `/speckit.analyze` — STRICTLY READ-ONLY by its own spec: it writes zero bytes, so no file on disk can prove it ran
- `/speckit.implement` — its only claim of completion is every task marked [X] in tasks.md, the agent grading its own homework
- `/speckit.converge` — a clean converge is forbidden to touch tasks.md, so the converged outcome is invisible to the filesystem
- the ritual in `.multivac/ritual.md`, printed by `change close`
<!-- multivac:end -->
```

## With nothing declared

The three headings remain. Under **Gate** and **Automatic**, the lifecycle's own
entries; and a line saying the adapter groups are empty because no adapter is
declared.

## With an unverified adapter

```markdown
- `acme-graph` is declared but unknown to this tool — declare its contract under `graphers:` or nothing will run
```

Never a guessed obligation.

## What must never appear

An invariant identifier. Identifiers are allocated from each brain's own table,
so a generated one names a different rule, or none, in every other ecosystem.
MV-96 carries an `absent` leg over the renderer.
