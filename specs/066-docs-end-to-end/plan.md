# Implementation Plan: docs-end-to-end

## Summary
- **Code.** `init` calls `writeEcosystem`. `reposList` and `doctor`'s `reposLine` use `cloneState` and `cloneFix`.
- **Docs.** Edits to `skills/multivac/SKILL.md`, `references/change.md`, `references/verify.md` (and the `.claude` copy), `site/content/docs/guide/getting-started.md`, `running-changes.md`, `concepts/brain-driven-development.md`, `concepts/the-change.md`, `reference/graphers-and-sdd.md`, `configuration.md`, `commands.md` samples, `DESIGN.md`, and `README.md`.
- **Law.** MV-141, with `absent` legs on the retired sentences, and a note on MV-132.
