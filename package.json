{
  "name": "multivac",
  "version": "1.0.0",
  "private": true,
  "description": "Brain-driven development CLI",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "multivac": "dist/cli.js",
    "mvac": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "test": "npm run build && node --test \"dist-test/**/*.test.js\"",
    "preinstall": "npx only-allow pnpm"
  },
  "dependencies": {
    "picomatch": "^4.0.2",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/picomatch": "^3.0.1",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@11.21.0"
}