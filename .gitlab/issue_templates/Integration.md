## The tool

<!-- Which coding agent, grapher or SDD tool. -->

## What it reads

<!-- The exact path and format, from the vendor's own documentation. Link it.
     Examples: a plain AGENTS.md at the repo root; a file with YAML
     frontmatter; a JSON fragment merged into a settings file. -->

- Instruction file:
- Format: <!-- reads AGENTS.md natively / symlink / stub with frontmatter / json fragment -->
- Hook mechanism, if any:
- Skill or extended-instruction location, if any:
- Primary source:

## Why it cannot be verified, if it cannot

<!-- If the vendor does not document the format, say so. An entry marked
     unsupported with a reason is a valid contribution — a guessed one is not.
     multivac exists to stop documentation from lying about code; its own
     registry is held to the same standard. -->
